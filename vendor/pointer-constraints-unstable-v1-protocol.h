/* Minimal shim of the pointer-constraints-unstable-v1 protocol header.
 *
 * wlroots' <wlr/types/wlr_pointer_constraints_v1.h> #includes this file
 * solely for `enum zwp_pointer_constraints_v1_lifetime` (the constraint's
 * `lifetime` field). The full header is normally generated by
 * wayland-scanner from wayland-protocols' XML, but teruwm only needs the
 * enum, so we declare it directly — same pattern as
 * vendor/cursor-shape-v1-protocol.h and vendor/xdg-shell-protocol.h.
 * vendor/ is already on the -I path for build.zig.
 *
 * Values match the protocol XML:
 * https://gitlab.freedesktop.org/wayland/wayland-protocols/-/blob/main/unstable/pointer-constraints/pointer-constraints-unstable-v1.xml
 */

#ifndef MIOZU_POINTER_CONSTRAINTS_UNSTABLE_V1_PROTOCOL_H
#define MIOZU_POINTER_CONSTRAINTS_UNSTABLE_V1_PROTOCOL_H

#include <stdint.h>
#include <wayland-server-core.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Forward declarations — wlroots only uses pointers to these. */
struct zwp_pointer_constraints_v1;
struct zwp_locked_pointer_v1;
struct zwp_confined_pointer_v1;

#ifndef ZWP_POINTER_CONSTRAINTS_V1_LIFETIME_ENUM
#define ZWP_POINTER_CONSTRAINTS_V1_LIFETIME_ENUM
enum zwp_pointer_constraints_v1_lifetime {
	ZWP_POINTER_CONSTRAINTS_V1_LIFETIME_ONESHOT = 1,
	ZWP_POINTER_CONSTRAINTS_V1_LIFETIME_PERSISTENT = 2,
};
#endif /* ZWP_POINTER_CONSTRAINTS_V1_LIFETIME_ENUM */

#ifdef __cplusplus
}
#endif

#endif /* MIOZU_POINTER_CONSTRAINTS_UNSTABLE_V1_PROTOCOL_H */
