# teru + teruwm — FINAL audit status (Zig 0.17.0-dev.1417)

_2026-07-19. Every source file reviewed line-by-line across two runs; all critical/high/medium findings adversarially verified._

## Fixes applied this session (all build clean, all tests pass)

- **src/png.zig** — Zig-upgrade build fix: std.hash.crc.Crc32 → std.hash.Crc32  _(verified: build)_
- **src/compat.zig + src/main.zig + src/compositor/main.zig** — SIGPIPE ignored process-wide (ignoreSigpipe) — broken clipboard pipe / MCP client socket no longer kills the process  _(verified: regression test)_
- **src/render/FontAtlas.zig** — removed double-free of font_data on stbtt_InitFont failure  _(verified: build+tests)_
- **src/compositor/WmMcpTools.zig** — resolveNode uses satNode(nid) not @intCast — negative node_id no longer aborts compositor  _(verified: build+tests)_
- **src/agent/McpTools.zig** — extractNestedJsonBool/Int fixed double-add OOB on crafted MCP JSON (top-level fallback) — was a crash  _(verified: regression test)_
- **src/compositor/WmConfig.zig + ServerConfig.zig** — load/loadWithLibc fill in place — self-referential slices (bar formats, workspace-startup, xkb) no longer dangle by-value; hardens live reload  _(verified: build+tests)_
- **src/compositor/Server.zig + WmMcpServer.zig + main.zig** — releaseEventSources removes bar-exec + wm_mcp event sources BEFORE wl_display_destroy — shutdown UAF gone  _(verified: headless SIGTERM shutdown, clean)_
- **src/modes/windowed.zig** — vi-cursor overlay guarded against reversed cx..max_x / cy..max_y range → no panic when cursor outside pane  _(verified: build+tests)_
- **src/render/Compositor.zig** — selection screen_row skips u16 overflow on very deep scrollback → no @intCast panic  _(verified: build+tests)_

### Criticals: 12 found → 11 fixed, 1 left open
- OPEN: **`src/platform/windows/platform.zig:522`** — WM_SIZE takes low 32 bits of lParam not low 16. Windows-only, and Windows support is deferred (per project state); left untouched since it can't be verified on this Linux host.

### Highs: 54 verified → 47 confirmed, 5 downgraded to medium (still real), 2 refuted
The 2 refuted: `PaneBackend.zig:332` blocking-write premise was wrong (fd is set non-blocking one level up in Pane.init); `Bar.zig:485` shutdown UAF — refuted because the Server.releaseEventSources fix above resolves it.

## Open high-severity backlog (verified real, NOT yet fixed)

Grouped by subsystem. These need decisions / hardware verification (many are behavior changes in the compositor render + input paths). Severity in brackets where downgraded.

### agent/mcp
- **`src/agent/forward.zig:92`** — forwardRequest does blocking connect/write/read-until-EOF with no timeout, and it runs synchronously on teru's single-threaded main loop for every teruwm_* tool call
  - ipc.connectPosix (src/server/ipc.zig:232-247) never sets O_NONBLOCK or any SO_RCVTIMEO/SO_SNDTIMEO on the client socket, so forwardRequest's connect (forward.zig:94), two writes (103-104), and the read loop (109-113, which only terminates when teruwm CLOSES the connection) are all fully blocking. forwardRequest is wired as the framework forward fn for the teruwm_ prefix (src/agent/McpServer.zig:294-296) and is invoked from McpServer.poll() -> handleRequest, which is called from the windowed-mode main loop (src/modes/windowed.zig:1353) — the same loop that drains every pane's PTY, processes input, and renders. Any teruwm_* request arriving on teru's MCP socket therefore freezes ALL of teru (every pane, keyboard input, rendering) for the full duration of the compositor's tool execution (screenshots, restart), and indefinitely if teruwm is wedged, SIGSTOPped, or accepts but never closes. This is a recurrence of the codebase's known bug class #1 (blocking call starving a single-threaded loop — the exact mechanism of the historical touchpad freeze, now on the teru side). teruwmctl/main.zig and McpBridge.zig also call it but those are standalone processes where blocking is fine.
- **`src/agent/McpFramework.zig:225`** — Attacker-controlled Content-Length causes usize integer-overflow panic in the http-framing read loop, aborting teruwm (the display server) in Debug/ReleaseSafe builds
  - handleRequestFd's termination check does `if (total >= body_start + cl) break;` where `cl` comes from tools.parseHttpContentLength (McpTools.zig:285-290), which parses an unbounded usize. A local same-UID client sending `POST / HTTP/1.1\r\nContent-Length: 18446744073709551615\r\n\r\nx` makes `body_start + cl` overflow usize. In Debug and ReleaseSafe builds (make compositor is Debug; the daily-driver teruwm is a dev build) Zig's overflow check panics and the whole compositor aborts — total session loss for every window. The .http framing is exactly the one teruwm uses (WmMcpServer.zig:332 sets .framing = .http and :343 calls F.handleRequestFd). teru's own socket uses .line_json and is unaffected. In ReleaseFast it wraps silently and merely truncates, which masks the bug in release testing.
- **`src/agent/McpServerTools.zig:288`** — toolReadOutput: per-row line_end initialized to 0 instead of text_pos — a row with no ASCII-printable cells rewinds text_pos to 0 and silently discards ALL previously extracted rows
  - In the row loop (lines 286-312), `var line_end: usize = 0;` (line 288) is only updated when a cell with cp in [32,127) is written. The trim step `text_pos = line_end;` (line 306) is meant to rewind to the end of this row's last printable char, but for a row containing zero ASCII-printable cells — e.g. a full-width box-drawing rule (U+2500 '─', printed constantly by Claude Code, htop, mc, any TUI separator), a CJK-only row, or block-character bars — no branch fires (cp >= 127 cells match neither arm; cp==0 trailers write a space but do not update line_end... a row of pure box-drawing writes nothing at all), so line_end stays 0 and text_pos is reset to 0, erasing every row accumulated before it. teru_read_output on a pane running any TUI with separator lines returns only the content BELOW the last such line. Verified against Grid.zig: Cell.char defaults to ' ' (0x20, caught by the first branch), which is why ordinary rows mask the bug; wide-glyph trailer cells are char==0 (Grid.zig:557).
- **`src/agent/PaneBackend.zig:332`** — handleWrite passes the raw JSON-escaped bytes to the PTY — \n, \r, \", \\ arrive as literal backslash sequences, so the write op cannot send Enter at all
  - extractJsonString (McpTools.zig:85-108) returns the slice between the quotes verbatim, escapes included (it only skips over `\x` pairs to find the closing quote). handleWrite passes that slice straight to pane.ptyWrite (line 332). A client sending {"data":"ls\n"} therefore delivers the two bytes '\\','n' to the shell instead of a newline; quotes and backslashes are similarly mangled. Since a raw newline byte inside the JSON string would split the NDJSON frame (poll() dispatches on '\n', line 153), there is no way whatsoever to transmit Enter through this protocol — the core spawn-agent-then-drive-it flow is broken. The sibling terminal-MCP path proves the intended pattern: toolSendInput (src/agent/McpServerTools.zig:390-393) runs tools.unescapeJson into a stack buffer before ptyWrite, with an explicit comment that unescaping never grows the data. PaneBackend simply skipped that step. The spawn metadata fields (name/group/role, lines 242-244) are stored un-unescaped too.
- **`src/agent/McpFramework.zig:366`** — teruwm_* tool forwarding performs a blocking, timeout-less connect/write/read to the compositor socket from inside teru's single-threaded daemon poll loop
  - handleToolsCall's forward path calls `fwd.fn_(body, resp)` which is forward.forwardRequest (wired at McpServer.zig:294-296). forwardRequest (forward.zig:92-123) uses ipc.connect — and connectPosix (ipc.zig:232-247) never sets O_NONBLOCK, unlike acceptPosix which does — then does header write, body write, and a read-until-EOF loop with no deadline. This runs inline in the daemon loop (daemon.zig:254 m.poll() → handleRequest → dispatch), the same loop that drains every pane PTY and relays output to the attached client. A slow teruwm tool (teruwm_screenshot is a known multi-hundred-ms synchronous operation) freezes all of teru — PTY relay, client input, everything — for the full round-trip; a wedged teruwm hangs the teru daemon indefinitely since the blocking read only returns when teruwm responds or closes. This is the same starvation class as the historical touchpad-freeze bug, on teru's side.
- **`src/agent/PaneBackend.zig:368`** — handleCapture: a row containing zero ASCII-printable cells resets text_pos to 0, silently discarding every previously captured row
  - `var line_end: usize = 0;` is declared per-row (line 368) but holds an absolute buffer position, and line 385 does `text_pos = line_end;` after each row. line_end is only advanced by the `cp >= 32 and cp < 127` branch. Grid cells default to ' ' (0x20, Grid.zig:27) so genuinely blank rows are safe (spaces take the printable branch), but any row whose every cell is cp >= 127 — a full-width box-drawing separator (U+2500 runs, exactly what Claude Code's own TUI draws), a CJK-only row, an emoji row — never sets line_end, so text_pos snaps back to 0 and everything captured so far is overwritten. Capture on a real agent pane returns only the content after the LAST such row. Additionally the trim logic is otherwise dead: because ' ' (32) is caught by the printable branch and sets line_end, trailing spaces are never trimmed and every line is padded to full grid width; the `cp == ' '` half of the else-if on line 378 is unreachable.

### config
- **`src/config/Config.zig:173`** _(→medium)_ — Documented `tab_width` config key is never parsed — silently ignored, always defaults to 8
  - The field `tab_width: u8 = 8` exists (Config.zig:173) and is fully plumbed to consumers (src/modes/windowed.zig:385 and src/modes/daemon.zig:172 via mux.spawn_config, src/compositor/ServerConfig.zig:82, src/core/Pane.zig:150/220, src/core/Grid.zig:304, used by VtParser.zig:378 for tab stops), and docs/CONFIGURATION.md documents it three times (lines 67, 226, 235 — including a worked example `tab_width = 4`). But `applyField` (Config.zig:519-647) has no `tab_width` branch, so a user setting the documented key gets no effect and no error. Verified by grepping every `tab_width` reference in src/ — no parser reads it anywhere.
- **`src/config/SessionDef.zig:604`** — findNodeName/paneExistsByRole match multiplexer pane ids against ProcessGraph node ids — two unrelated id spaces — so saved roles are cross-attributed and the documented restore idempotency breaks
  - findNodeName (SessionDef.zig:600-607) compares `node.id == pane_id` where pane_id comes from `ws.node_ids.items` (Multiplexer.next_pane_id space, Multiplexer.zig:227-229) and node.id is ProcessGraph's own auto-increment counter (ProcessGraph.zig:70, 124, 154). Nothing links them: plain keyboard `spawnPane` (Multiplexer.zig:204) never registers a graph node at all, while PaneBackend.zig:255, McpServerTools.zig:441, and SessionDef.restore itself (line 370) spawn graph nodes whose ids only align with pane ids by coincidence when both counters happen to be in lockstep. Consequences: (1) saveFromLive line 260 records as a pane's `role` the name of whatever unrelated graph node numerically shares its pane id — e.g. a user shell pane can be saved with an agent's role and vice versa; (2) paneExistsByRole (lines 589-597) uses the same broken mapping, so the module's stated contract ('IDEMPOTENT: panes matched by role, never duplicated', lines 4-5 and 300-301) fails — a second restore duplicates every pane whose graph correlation is off, or wrongly skips a pane that falsely matches. The `graph.spawn(...) catch {}` at line 375 makes this worse: a failed registration silently guarantees the next restore duplicates that pane.
- **`src/config/Keybinds.zig:679`** — Default Mod+Tab / Mod+Shift+Tab / Mod+Escape bindings are stored as raw XKB keysyms (0xFF09/0xFF1B) that neither dispatch path can ever deliver — three documented default chords are silently dead in BOTH binaries
  - loadDefaults stores pane_focus_next/pane_focus_prev under key 0xFF09 (lines 679-680) and workspace_toggle_last under 0xFF1B (line 667). But both callers of Keybinds.lookup normalize before lookup: teruwm's ServerInput.zig:400-408 folds sym 0xff09→'\t' (0x09), 0xff1b→0x1b, 0xff0d→'\r', 0xff08→0x7f; standalone windowed.zig:921 builds ks_char:u8 that maps ANY keysym outside 0x20-0x7F (except Return→'\r') to 0. So the stored 0xFF09/0xFF1B values can never match — verified there is no other Tab/Escape chord path (grep of windowed.zig and ServerInput.zig). docs/KEYBINDINGS.md:34 documents '$mod+Tab / $mod+Shift+Tab | Focus next/previous' and :68 documents '$mod+Escape | Toggle last visited workspace' as working. The same store/match mismatch kills user-config named keys: namedKey (lines 422-428) maps 'enter'/'return'→0xFF0D, 'esc'/'escape'→0xFF1B, 'tab'→0xFF09, 'backspace'→0xFF08 — all dead in teruwm (folded to ASCII before lookup) and dead standalone (u8 truncation); meanwhile 'delete' (0xFFFF), arrows, and F-keys pass through unfolded so they work ONLY in teruwm, never standalone. The working convention is the ASCII form — line 688 binds spawn_terminal on '\r' and line 776 binds prefix-Esc on 0x1b, both of which do match. (Shift+Tab is doubly dead in teruwm: xkb delivers ISO_Left_Tab 0xFE20, also unfolded.)
- **`src/config/Keybinds.zig:831`** — loadTerminalZoomDefaults binds zoom_in on (mod, '_') WITHOUT shift, but producing '_' requires Shift and both key paths include shift in mods — the documented $mod+_ zoom-in chord never fires, leaving standalone teru with no working keyboard zoom-in at all
  - Line 831: `self.add(n, M, '_', .zoom_in)` stores mods = {mod} only. A real press is Mod+Shift+minus: xkb delivers keysym '_' with Shift held; KeyHandler.platformMods (KeyHandler.zig:188) sets mods.shift from SHIFT_MASK, so lookup receives {mod, shift} vs stored {mod} → Mods.eql fails → no match. lookup's normalization folds only A-Z and the shifted number row, not '_'. The file's own convention proves the fix: line 732 binds Shift'd quotedbl WITH shift (`self.add(n, MS, '"', .compositor_quit)`), and the adjacent test at line 1017-1021 verifies exactly that pattern. docs/KEYBINDINGS.md 'Font zoom' table documents `$mod+_` = 'Zoom in (+1 px) — shift + minus, same key'. Impact is total for keyboard zoom-in in standalone teru: the legacy handleGlobalKey '='→zoom_in fallback (KeyHandler.zig:320) has zero production callers, and Mod+'=' is already taken by resize_grow_w (line 714), so only the Alt+scroll mouse gesture can zoom in.

### core
- **`src/core/KeyHandler.zig:102`** — Prefix-key focus-next/prev and workspace-switch return .none, so the caller never repaints — the screen stays stale after these commands.
  - In handleMuxCommand the prongs 'n' => mux.focusNext() (line 102), 'p' => mux.focusPrev() (line 103), '1'...'9' => mux.switchWorkspace(cmd - '1') (lines 135-138) and '0' => mux.switchWorkspace(9) (lines 140-142) all fall through to `return .none;` (line 174) instead of returning `.panes_changed`. The only caller, src/modes/windowed.zig:1079 and its no-xkb fallback at :1134, sets `force_redraw = true` and marks every pane's grid dirty ONLY when `action == .panes_changed`. focusNext/focusPrev/switchWorkspace (Multiplexer.zig) call `markDirty()`, but markDirty merely sets `persist_dirty` for session autosave — it does not set any render/grid dirty flag, and windowed.zig's render gate `any_dirty = had_output or force_redraw or (per-pane grid.dirty)` never consults a mux-level flag. Net effect: after the tmux-style prefix key (Ctrl+Space) followed by n/p (focus move → active-pane border highlight) or 1-9/0 (workspace switch → an entirely different set of visible panes), nothing repaints until the next PTY byte arrives on some pane. Workspace switch appears to do nothing. The config-driven executeAction path in the same file correctly returns .panes_changed for the identical actions (lines 209-210 for focus, 242-248 for workspace), confirming these prefix-path prongs were meant to as well.
- **`src/core/Clipboard.zig:196`** — writeAllToPty blocks the calling thread in posix.poll for up to 1s draining the PTY master; on teruwm this is the single wl_event_loop thread, so a slow PTY reader freezes all compositor input.
  - writeAllToPty (lines 175-210) retries short PTY writes and, on EAGAIN, calls `posix.poll(&fd_array, remaining_ms)` (line 196) with remaining_ms up to the 1000ms deadline (lines 176,189-191). This is synchronous: while poll blocks waiting for POLLOUT, the caller's thread does nothing else. In teruwm the call chain ServerClipboard.finishPaste/pasteInternal -> teru.Clipboard.pasteText -> writeSanitised -> writeAllToPty (ServerClipboard.zig:67,188) runs on the compositor's single wl_event_loop thread — finishPaste fires from the pasteReadable fd callback, pasteInternal from key handling. If the pane's foreground child stops reading stdin while a >kernel-buffer (~4-16KB up to 64KB) paste is in flight (e.g. pasting a large blob into an app that is momentarily busy / not reading), the PTY master input buffer fills, every write returns EAGAIN, and poll blocks — starving libinput and freezing cursor/keyboard/rendering for up to the full 1s deadline. This is exactly the known single-threaded-loop starvation class (nanosleep / sync readback / blocking drain). Notably the paste READ side was deliberately made async via wl_event_loop_add_fd + O_NONBLOCK (ServerClipboard.paste), but the write side stayed synchronous. The 1s deadline bounds the freeze but a full 1-second input freeze on a routine paste is user-visible.
- **`src/core/Multiplexer.zig:279`** — closePane calls ws.removeNode before ws.removeNodeFromTree, which kills the tree's active-node reselect — closing the active pane in a split-tree workspace leaves NO focused pane and keystrokes are silently dropped
  - In closePane (lines 279-282) each workspace gets `ws.removeNode(pane_id)` then `ws.removeNodeFromTree(pane_id)`. Workspace.removeNode (Workspace.zig:146-148) nulls `active_node` when it equals the removed id. Workspace.removeNodeFromTree's reselect logic (Workspace.zig:423-431) only picks a new first-leaf active_node `if (self.active_node) |active| active == pane_id` — but active_node is already null by then, so the reselect is dead code in this call order. Since getActiveNodeId() returns `active_node` whenever `split_root != null` (Workspace.zig:271-272), the workspace ends with no active pane. Repro: prefix-c split (windowed.zig:963 addNodeSplit builds a tree), then prefix-x close (KeyHandler.zig:89-101) — getActivePane() is null afterward, so typed input goes nowhere and no active border is drawn until the user presses focus-next (focusNextInTree recovers from null) or clicks. Same path fires from daemon .close_pane (daemon.zig:493), MCP teru_close_pane (McpServerTools.zig:581), and the dead-shell reap sweep (windowed.zig:1306) — a shell exiting inside a split also drops focus. The existing test (line 698) only covers the flat-list path, where active_index clamping auto-focuses a neighbor.
- **`src/core/ViMode.zig:280`** — getChar returns ' ' for every scrollback row, so the w/b/e word motions teleport the cursor out of scrollback — 'w'/'e' scan up to sb_lines*cols (~8M iterations at the 100k-line default cap) and land in the live grid; 'b' jumps to (0,0)
  - With the cursor on any scrollback row (vrow < sb_lines), getChar (ViMode.zig:267-281) unconditionally returns ' '. wordForward's skip-spaces loop (297-307) then walks every remaining scrollback cell (col wrap + row increment) until it reaches the live grid's first non-space — at Scrollback's default `max_lines = 100_000` (Scrollback.zig:65) and 80-200 cols that is millions of iterations on the input path of the single-threaded windowed event loop (a perceptible stall in Debug), and the cursor lands rows/thousands-of-rows away from where the user was, destroying their position and any in-progress visual selection anchor. wordBackward (326-336) symmetrically walks all the way back and parks the cursor at absolute (0,0). Since vi mode exists precisely for scrollback navigation, all three word motions are unusable in the region they matter most. The justifying comment (lines 276-279: "we can't easily access individual characters... the Grid... doesn't carry scrollback line data directly") is factually wrong: Grid has a `scrollback: ?*Scrollback` field — the very same caller dereferences `pane.grid.scrollback` for yank (windowed.zig:718), and Selection.stripSgrToColumns (Selection.zig:210) already implements per-column text extraction from stored scrollback lines.
- **`src/core/Grid.zig:864`** — resize error paths: alt_cells double-free/UAF and inconsistent dims->OOB when an allocation fails mid-resize
  - Two broken error paths in resize(). (1) Grid.zig:863-867: `allocator.free(old_alt)` runs BEFORE `try allocator.alloc(Cell, new_total)`; if that alloc fails, the function returns error with self.alt_cells still pointing at freed memory — deinit (line 375-378) then double-frees it, and a later switchToAltScreen swaps the freed buffer in as the active screen (UAF). (2) Grid.zig:836-847: the old cells are freed and replaced (lines 836-837) before the row_meta `try allocator.alloc(RowMeta, new_rows)` at line 840; on failure, self.cells has the NEW size but self.rows/self.cols keep the OLD values (assigned only at lines 849-850), so on a shrink the next cellAt/clearRow indexes past cells.len (panic in Debug/ReleaseSafe, UB in ReleaseFast). Multiplexer.zig:498 catches the resize error with only a log warning and keeps using the pane, so the corrupted grid stays live. OOM-only reachability, but the invariant is alloc-before-mutate.
- **`src/core/Grid.zig:884`** — markRowDirty silently destroys the documented external `grid.dirty = true` all-dirty sentinel, losing full-repaint requests
  - clearDirty (Grid.zig:900-904) sets the inverted range min=rows/max=0 so that external code may request a full repaint by setting `grid.dirty = true` directly; renderDirtyWithSelection honors this (src/render/software.zig:189 `if (grid.dirty_row_min > grid.dirty_row_max)` -> full render). But markRowDirty's else-branch (Grid.zig:884-887) collapses that sentinel to a single row: after an external `dirty = true`, the first PTY byte written in the same frame narrows the range to cursor_row..cursor_row and only that row repaints. The reverse ordering also fails: if PTY output already set a tracked range (e.g. 5..5), a later external `.dirty = true` is a no-op and its full-repaint intent is dropped entirely. Raw `.dirty = true` sites that rely on the sentinel: src/input/mouse.zig:251,255,260,363,419,459,474,614,669 (selection/scroll UI changes), src/agent/McpServerTools.zig:624,830,909, src/core/Pane.zig:319, src/modes/common.zig:368,489. Any frame in which both PTY output and one of these fire leaves stale pixels (e.g. selection highlight remnants) — the same user-visible class as the historical half-render bugs. teruwm's compositor code is unaffected (it correctly calls markAllDirty), but teru's TUI/windowed frontends are exposed.
- **`src/core/VtParser.zig:1089`** — DECSET/DECRST (CSI ? Pm h/l) honors only params[0]; combined mode lists are silently dropped — breaks ncurses mouse enable via terminfo XM
  - dispatchCsiPrivate does `const mode = self.getParam(0, 0);` and the 'h' (line 1091) and 'l' (line 1122) arms switch on that single value. Per xterm, Pm is a semicolon-separated LIST and each mode must be applied. teru advertises TERM=xterm-256color (src/pty/PosixPty.zig:230), and the system terminfo's extended mouse cap is `XM=\E[?1006;1000%?%p1%{1}%=%th%el%;` (verified via infocmp on this machine) — so every ncurses app that enables the mouse (htop, mc, tig, ...) sends the combined `ESC[?1006;1000h`. teru sets mouse_sgr=true from params[0]=1006 and drops the 1000, so mouse_tracking stays .none and mouse reporting never activates. The matching combined disable on teardown is half-processed the same way. This is a silent, user-visible functional failure of mouse support in a whole app class.

### input
- **`src/input/mouse.zig:120`** — All cell coordinates are computed window-globally but consumed pane-locally, breaking PTY mouse reporting, drag selection, word-select, and Shift+click URL open in every multi-pane layout
  - reportMousePress/Release/Motion (mouse.zig:120-121, 156-157, 191-192) compute mcol/mrow as (mouse.x - lp.padding)/cell_width clamped to the FULL-WINDOW grid dims (windowed.zig:1161-1178 passes raw pixels and window grid_cols/grid_rows), then write those coords to the ACTIVE pane's PTY. Pane grids are pane-local and offset: Multiplexer.resizePanePtys (Multiplexer.zig:486-488) sizes each grid to its inset layout rect. So vim with mouse tracking in the right half of a vertical split receives window-global columns (e.g. col 60 instead of pane-local col 20). The same window-global row/col feeds selection.selectWord (mouse.zig:458), selection.begin/update (636, 664, 547), handleUrlClick→grid.cellAtConst (215-235, clamped so no crash but wrong cell), and updateUrlHover (242-243). The renderer interprets selection coords pane-locally (render/Compositor.zig renderPaneIntoRect iterates 0..grid.rows/cols and calls sel.isSelected with the pane-local row/col), so in a split the drag-selection highlight and the copied text are offset by the pane's origin. Only the single-pane case (pane rect origin == padding) lines up. Note click-to-focus and border drag correctly work in pixel space against layout rects — only the cell-space consumers are wrong. Also, since reportMousePress runs before click-to-focus and returns consumed when the active pane has tracking on (mouse.zig:407-409), clicking another pane while vim is focused both misroutes the event and prevents focus change.
- **`src/input/TuiInput.zig:188`** — When the prefix is armed, an incoming escape sequence has its ESC byte swallowed by handlePrefixKey and the CSI tail is forwarded to the shell as literal text
  - In .ground with prefix_active (TuiInput.zig:184-200), the next byte is unconditionally consumed by handlePrefixKey. handlePrefixKey(0x1B) has no case → .none → the ESC byte is dropped (only non-.none actions dispatch; the byte itself is never forwarded, raw_start skips it at 191). The remaining bytes of the sequence then flow through .ground as ordinary raw input and are flushed to the daemon as active_input (316-319). Concrete: press Ctrl+B then Up-arrow (ESC [ A) within the 500 ms prefix window → the shell receives literal "[A". Prefix then a mouse click under SGR mouse mode (ESC [ < 0;50;20 M) → "[<0;50;20M" is typed into the active pane. tmux muscle-memory (prefix+arrow = select-pane) makes this an everyday keystroke pattern.

### macos
- **`src/platform/macos/platform.zig:220`** — setTitle casts a non-NUL-terminated slice to [*:0]const u8, causing stale-title display and out-of-bounds read on OSC-set titles
  - setTitle (lines 219-224) does `@as([*:0]const u8, @ptrCast(title.ptr))` and hands it to stringWithUTF8String:, which reads until a zero byte. The only dynamic caller is src/modes/windowed.zig:1432: `win.setTitle(pane.vt.title[0..pane.vt.title_len])`. VtParser (src/core/VtParser.zig:737-739) @memcpy's the OSC 0/2 payload and sets title_len WITHOUT writing a terminator, so after a shorter title replaces a longer one, stringWithUTF8String: reads past title_len and displays stale bytes of the old title. Worse: a hostile/legitimate 256-byte title fills the entire [MAX_OSC_LEN]u8 array leaving no NUL inside it, so the read runs past the array into adjacent VtParser struct memory until it happens upon a zero byte. Any program printing ESC]0;...BEL reaches this path. MacosWindow.init (line 184) has the same latent cast but is only called with the literal "teru" (windowed.zig:314), which is NUL-terminated in rodata.
- **`src/platform/macos/platform.zig:233`** — No autorelease pool exists anywhere on the macOS path — every pollEvents call and every putFramebuffer frame leaks autoreleased ObjC objects
  - Grep across the whole repo and src/platform/macos/ (platform.zig + keyboard.zig) finds zero objc_autoreleasePoolPush/objc_autoreleasePoolPop. pollEvents creates an autoreleased NSString via stringWithUTF8String: (lines 233-235) BEFORE the nil-event check, so it allocates on every single call — and windowed.zig:573 drives `while (win.pollEvents())` every frame of the main loop. nextEventMatchingMask: returns an autoreleased NSEvent per event. putFramebuffer additionally autoreleases the NSDeviceRGBColorSpace NSString every frame (lines 371-373). With no pool in place, the ObjC runtime's autoreleaseNoPool path leaks each of these objects permanently (plus logs). Net effect: unbounded memory growth proportional to frames rendered and events processed for the lifetime of the macOS window. (The alloc/init'd bitmap and image ARE explicitly released at lines 426-427; only the autoreleased objects leak.)
- **`src/platform/macos/platform.zig:377`** — putFramebuffer feeds the renderer's ARGB u32 buffer to NSBitmapImageRep as meshed RGBA — red and blue channels are swapped on macOS
  - The software renderer emits ARGB u32 pixels (src/render/software.zig:81, :550 "Pack R, G, B bytes into an ARGB u32") and windowed.zig:1627 passes cpu.getFramebuffer() straight through — no macOS swizzle exists anywhere (grepped). On little-endian (incl. Apple Silicon) an ARGB u32 is bytes [B,G,R,A] in memory. The initWithBitmapDataPlanes:... call (lines 377-388, samplesPerPixel=4, hasAlpha=YES, isPlanar=NO, no bitmapFormat: parameter) declares default meshed format = samples in R,G,B,A byte order, alpha last. AppKit therefore reads the B byte as red and the R byte as blue: every colored glyph/theme color renders with red and blue exchanged (blue ANSI text shows red, etc.). Alpha lands correctly (byte 3), and grayscale/monochrome content is unaffected — which is why this survives casual testing on a default gray-on-black theme.
- **`src/platform/macos/keyboard.zig:391`** — Modifier state is never fed on macOS: updateModifiers has no macOS caller and modifier keycodes never reach updateKey, so Shift/Ctrl/CapsLock are permanently false — uppercase, shifted symbols, and Ctrl-chars cannot be typed.
  - updateModifiers (line 391) is documented as taking NSEvent.modifierFlags, but its ONLY caller in the tree is the .wl_modifiers event arm (src/modes/windowed.zig:618), and the only emitter of that event is src/platform/linux/wayland.zig:1053 — macOS never emits it. The other feeding path, updateKey (line 368), is called from .key_press/.key_release (windowed.zig:697/1152), but macOS delivers modifier-key transitions exclusively as NSEventTypeFlagsChanged — src/platform/macos/platform.zig defines the constant (line 117) yet its pollEvents switch (lines 266-355) never handles it, so keycodes 0x36-0x3E never arrive. Net effect verified end-to-end: mods and caps_lock stay at their all-false defaults forever, so processKey (lines 427-437) always takes the unshifted table and never the ctrlChar path — Shift+A emits 'a', Shift+1 emits '1', Ctrl+C emits plain 'c' (no SIGINT), and getKeysym (line 359) always returns unshifted keysyms, breaking shifted-key matching upstream. Every shift/ctrl/caps test in this file passes only because it calls updateKey(kVK_Shift, ...) directly — a delivery the real platform never makes, so the test suite green-lights dead plumbing.

### modes
- **`src/modes/daemon.zig:276`** — runSessionAttach output relay drops bytes: single un-looped std.c.write(1, ...) on an fd made non-blocking as a side effect of O_NONBLOCK on stdin
  - Line 239-241 sets O_NONBLOCK on fd 0. stdin/stdout of a shell-launched process share one open file description for the tty, so fd 1 becomes non-blocking too — this exact phenomenon is documented (and correctly handled with a POLLOUT retry loop) in src/render/TuiScreen.zig:327-345 (writeAllFd) for the TUI path. runSessionAttach instead does `_ = std.c.write(1, payload.ptr, payload.len)` with payloads up to 64 KB: under output pressure (cat a large file, verbose build over SSH — the primary use case of this byte-relay mode) write returns short or EAGAIN, the unwritten tail is silently discarded, and the VT stream is truncated mid-escape-sequence, garbling the attached terminal.
- **`src/modes/windowed.zig:596`** — Focus-in/out events (ESC[I / ESC[O) are written to the active pane's PTY unconditionally, without tracking DECSET 1004 (focus reporting)
  - Lines 595-597 (focus_in) and 611-613 (focus_out) do `pane.ptyWrite("\x1b[I")` / `"\x1b[O"` on every window focus change. Per the xterm protocol these sequences must only be sent when the application has enabled focus reporting via `CSI ? 1004 h`. A grep of the whole repo for `1004`/`focus_report` confirms VtParser has no mode-1004 tracking anywhere — the writes are always-on. Apps that never enabled the mode receive stray bytes as keyboard input: `cat > file` records `^[[I` into the file, password/`read` prompts show garbage, and in vim (normal mode, focus events not enabled) the bytes parse as the real `[I` command ("show lines containing keyword under cursor"), visibly firing on every alt-tab. Contrast: `p.vt.app_cursor_keys` (line 851) shows the codebase already tracks the analogous DECCKM mode correctly.
- **`src/modes/windowed.zig:731`** — Pressing `/` in vi mode opens the search overlay but search input is unreachable — vi mode stays active and its intercept swallows every subsequent key
  - ViMode.handleKey('/') returns `.search` (ViMode.zig:160); the caller sets `search_mode = true` (windowed.zig:731 keysym path, 768 byte path) but never calls `vi_mode.exit()`. The vi intercept at windowed.zig:702 (`if (vi_mode.active)`) runs BEFORE the search-input handler at 777 and both of its paths end in `continue` (736/774), so while vi mode is active the `if (search_mode)` block can never execute. Result: the search overlay renders (line 1564) but every keystroke is still interpreted as a vi motion; typing a query moves the cursor around instead. The only way out is Escape — which exits vi mode AND resets scroll to 0 (line 712), discarding the scrollback position the user wanted to search from. Verified there is no earlier search_mode intercept (grep of all 12 references). KEYBINDINGS.md documents `/` = "Enter search" in the scroll/vi section, so this is a documented feature that cannot work.

### persist
- **`src/persist/Scrollback.zig:303`** _(→medium)_ — trimOldest wipes the ENTIRE scrollback (not the oldest chunk) whenever only one keyframe exists, which is the steady state for any configured scrollback_lines <= keyframe_interval (100)
  - With a single keyframe, next_kf_line = maxInt(u64) (lines 303-306), so the delta-removal loop (312-323) frees every delta, then the only keyframe is removed and total_lines is zeroed (360-361). Production panes always use keyframe_interval=100 (Pane.zig:153-156, 193-196, 223-226) and max_lines = spawn_config.scrollback_lines, which config parsing clamps only upward (Config.zig:551 — `@min(parseInt(...), 1_000_000)`, no lower bound). Trace with scrollback_lines=100: lines 0..99 fill; push of line 100 first runs the trim loop (pushLine:129) while only keyframe 0 exists → entire history erased; then lines 100..199 refill; push 200 erases again. User-visible effect: scrollback history periodically drops to zero instead of retaining the newest max_lines, and lineCount transiently exceeds max_lines between wipes (also breaking the max_lines contract). Default 10000-line config is unaffected, but any legal small config (<= 100) hits it deterministically.

### platform-linux
- **`src/platform/linux/keyboard.zig:158`** — updateModifiers passes the layout group as BOTH depressed_layout and locked_layout, doubling the effective layout index
  - `xkb_state_update_mask(self.state, depressed, latched, locked, group, 0, group)` — xkbcommon computes the effective layout as depressed_layout + latched_layout + locked_layout, then wraps it into range (verified signature in /usr/include/xkbcommon/xkbcommon.h:2021; the canonical Wayland-client pattern is `(…, 0, 0, group)`). Passing `group` twice yields an effective layout of 2*group. Caller chain verified: wayland.zig:1039-1058 forwards the raw wl_keyboard.modifiers group → windowed.zig:618 → updateModifiers. With a 2-layout config (e.g. us,ru), group=1 gives effective layout 2, which wraps to 0 — switching layouts in the compositor has NO effect in teru; with 3+ layouts the wrong layout is selected. Affects teru running as a Wayland client under any compositor for every multi-layout user.
- **`src/platform/linux/wayland.zig:992`** — The compositor-provided keymap fd is closed unread, so windowed Wayland teru decodes keys with the env-default layout instead of the compositor's configured layout
  - keyboardKeymap (lines 984-993) closes the wl_keyboard.keymap fd immediately ('we pass raw keycodes for now (xkbcommon later)'). The Wayland protocol makes this keymap authoritative — keycodes are only meaningful relative to it. keyboard.zig then builds its own keymap from XKB_DEFAULT_* env vars / system default (keyboard.zig:84-96, `xkb_keymap_new_from_names(ctx, null, 0)`), and windowed.zig:512-515 uses that env path whenever X11 info is unavailable (i.e., exactly the Wayland case; initFromX11 covers X11). Any user whose layout is configured in the compositor (GNOME/KDE/sway settings — dvorak, non-us, caps:escape options) and not mirrored in XKB_DEFAULT_LAYOUT gets wrong key decoding in Wayland-windowed teru. The modifier bit positions used by windowed.zig (SHIFT=1, CTRL=4, ALT=8, types.zig:57-60) are also only guaranteed by the compositor's keymap.
- **`src/platform/linux/wayland.zig:1113`** _(→medium)_ — mouse_motion events are pushed without the modifiers field, so Shift-modified motion features are dead on the Wayland backend
  - pointerMotion (line 1103-1114) pushes `.{ .mouse_motion = .{ .x = ..., .y = ... } }` — the Event's `modifiers` field (types.zig:33) defaults to 0. The X11 backend fills it (x11.zig:432 `.modifiers = @intCast(motion.state)`), and the consumer chain depends on it: windowed.zig:1235 passes `motion.modifiers` to handleMouseMotion, whose updateUrlHover (src/input/mouse.zig:241) gates the Shift+hover URL underline on `modifiers & SHIFT_MASK`, and the PTY motion-report path (modes 1002/1003) also receives 0. Result: Shift+hover URL detection never activates in Wayland-windowed teru, while it works on X11. The state needed is already tracked in the same struct (`state.mods_depressed`), and pointerButton (line 1134) already uses it.

### render
- **`src/render/FontSynth.zig:247`** — drawBlocks erases the font-rasterized shade glyphs U+2591-2593 (light/medium/dark shade) and never repaints them, so ░▒▓ render as blank cells
  - The loop `for (0x2580..0x25A0)` calls clearSlot() unconditionally at line 247 BEFORE the switch, but the switch has no cases for 0x2591-0x2593 (they fall to `else => {}` at line 299); the comment at line 267 says 'U+2591-2593 shade characters — use font glyph', yet the font glyph was just wiped. FontAtlas.zig rasterizes the whole 0x2580-0x259F range from the font (buildAtlasCodepoints lines 104-107) and then calls FontSynth.drawBlocks AFTER (FontAtlas.zig:421 and again in the zoom path at :609), so the erase always wins. No other code path special-cases shades (grepped software.zig/Ui.zig/FontAtlas.zig: zero hits for 2591/2592/2593); block_glyphs still holds GlyphInfo pointing at the now-all-zero atlas region, so the renderer blits fully transparent pixels. Empirically proven in a scratchpad copy: prefill atlas with 128, run drawBlocks, shade slots contain 0 nonzero pixels. Shades are heavily used (btop meters, dialog shadows, mc panels). Contrast with drawBoxDrawing, which correctly skips diagonals via the conn==0 check at line 213 BEFORE clearing.
- **`src/render/software.zig:256`** — Padding strip loop `for (pad..@min(grid_bottom, fb_h))` panics (safe builds) or writes wildly out of bounds (ReleaseFast) when the framebuffer is shorter than the 8px padding
  - renderRangeSel's padding fill (guarded only by `row_min == 0` at line 240) computes the left/right strip loop as `for (pad..@min(grid_bottom, fb_h))`. With `rows >= 1`, `grid_bottom = rows*ch + pad` always exceeds `pad`, so when `fb_h < pad` the range becomes e.g. `for (8..4)`. I verified on the exact installed toolchain (0.17.0-dev.1417) that a Zig for-range with start > end is an 'integer overflow' safety panic; in ReleaseFast the length wraps, `py` iterates far past `fb_h`, and the unchecked slices at lines 258/260 write outside the framebuffer allocation (memory corruption). Reachable in standalone windowed teru: src/modes/windowed.zig:623 calls `renderer.resize(sz.width, sz.height)` with the raw configure size and no minimum clamp, so shrinking the window below 8px tall (trivial under a tiling WM) crashes the next frame. teruwm is not affected (TerminalPane.zig:133-134 sizes the buffer as rows*ch + 2*pad, always > pad). The top fill (line 244) and bottom strip (line 250-251) already clamp correctly; only this loop misses the guard.

### server
- **`src/server/protocol.zig:154`** — recvMessage discards a partially-read 5-byte header, permanently desyncing the wire — the exact desync class writeAll was fixed for, but on the receive side
  - recvMessage does a single read of the 5-byte header via readNonBlock and bails with `if (hdr_n.? != header_size) return null` (protocol.zig:154), throwing away 1-4 already-consumed header bytes. The function's own comment (lines 166-169) explains why abandoning a committed frame desyncs the stream, and the body path was fixed to poll-and-resume — but the header path still has the hole. This is reachable: writeAll (protocol.zig:231-255) explicitly resumes across partial writes under backpressure (the documented SSH garble-under-load incident), so the peer's kernel buffer can legitimately contain only 3 of 5 header bytes at the moment the reader drains it. Once those bytes are discarded, the remaining 2 header bytes are parsed as the start of a fresh header and every subsequent frame is garbage until reattach. readNonBlock's doc comment ("read of exactly buf.len bytes", line 257) is also wrong — it is a single short-read-capable read.
- **`src/server/daemon.zig:493`** — Every pane closed via .close_pane (or any Multiplexer.closePane path) leaks a zombie process in the long-lived daemon
  - The .close_pane command calls mux.closePane → Pane.deinit → PosixPty.deinit (src/pty/PosixPty.zig:196-204), which sends SIGHUP, closes the master, and nulls child_pid — with no waitpid anywhere on that path. The daemon's only reaper, checkPaneAlive (daemon.zig:609), iterates mux.panes.items, and the closed pane has already been removed from that list, so its child is never waited on. There is no SIGCHLD handler or SIG_IGN in the non-compositor binary (grep confirms only SIGWINCH handlers). Result: one zombie per user-closed pane, accumulating for the life of the daemon — visible in ps and, pathologically, PID-space exhaustion. (Same latent flaw exists in windowed teru but the process is short-lived; the daemon is explicitly long-lived.)
- **`src/server/daemon.zig:552`** — sendStateSync does unclamped @intFromFloat on master_ratio — a corrupt/hand-edited .tsess ratio crashes the daemon on client attach
  - `@as(u32, @intFromFloat(ws.master_ratio * 100))` panics (Debug/ReleaseSafe — and `make release` ships ReleaseSafe) or is UB (ReleaseFast) when master_ratio is negative or enormous. That is reachable: SessionDef.parse does not clamp ratio (src/config/SessionDef.zig:199, e.g. `ratio = -5` parses fine) and SessionDef.restore writes it straight into the live workspace (src/config/SessionDef.zig:319 `ws.master_ratio = ws_def.ratio`), which runHeadless applies via applyTemplate before Daemon.run. The very first client attach then calls sendStateSync → panic → the daemon dies and takes every hosted agent PTY with it, violating the file's own "daemon must be uncrashable" invariant (daemon.zig:596-598). src/tiling/layouts.zig:29-33 documents this exact corrupt-.tsess threat and clamps at the layout site — sendStateSync missed the same defense.
- **`src/server/daemon.zig:634`** — checkPaneAlive removes a dead pane but never notifies the attached client — the client keeps rendering a ghost pane
  - When a shell exits on its own (user types `exit`, agent process ends), checkPaneAlive (daemon.zig:609-640) reaps the child, removes the pane from every workspace and from mux.panes, and calls mux.markDirty() — but sends neither a state_sync nor a pane_event to the connected client. sendStateSync is only called from tryAcceptClient, the .request_sync handler, and handleCommand (daemon.zig:521), and no client ever sends request_sync (grep over src/modes/* finds zero senders). So an attached client keeps displaying the dead pane frozen with stale content, and input routed to its pane_id is silently dropped, until the user happens to issue some other mux command. The protocol even defines Tag.pane_event (protocol.zig:29) and both clients decode it (src/modes/common.zig:378, src/modes/tui.zig:328), but the daemon never emits it.

### teruwm
- **`src/compositor/ServerCursor.zig:121`** — Alt+wheel zoom treats delta_discrete as notch count, but wlroots 0.18 delivers v120 units — one notch fires up to 120 zoomFont calls
  - The discrete zoom path does `var n = @abs(discrete); while (n > 0) : (n -= 1) _ = tp.zoomFont(target);` (lines 117-122). wlroots 0.18.3 fills wlr_pointer_axis_event.delta_discrete from libinput's v120 API, so one physical wheel notch arrives as ±120. The codebase itself knows this: Pane.axisScrollPixels (src/core/Pane.zig:102-105) documents "the v8 hi-res unit is 1/120th of a notch" and does `@divTrunc(discrete, 120)`, and its test (Pane.zig:519) uses 120 for a single notch. Each un-clamped zoomFont call performs a full FontAtlas.rasterizeAtSize + refont (TerminalPane.zig:720-750) synchronously on the single-threaded wl_event_loop, so one Alt+notch slams the font to the min/max clamp bound AND burns many full atlas rasterizations back-to-back — the known event-loop-starvation bug class (alt_scroll_zoom defaults to true, WmConfig.zig:312).
- **`src/compositor/ServerCursor.zig:143`** — Compositor wheel-scroll has no alt-screen guard and no mouse-reporting forwarding — scrolling in vim/htop drags the dead scrollback overlay over the TUI
  - Lines 143-196 unconditionally scroll the focused pane's scrollback whenever total_lines > 0. Windowed teru explicitly guards this: src/input/mouse.zig:492-494 (`// Don't scroll teru's scrollback when alt screen is active` — checks pane.vt.alt_screen) and forwards wheel events to apps with mouse tracking enabled via reportMousePress (mouse.zig:406-409, modes 1000/1002/1003). grep confirms zero alt_screen or mouse-report references anywhere in src/compositor/. Result on a teruwm-native pane: any full-screen TUI entered after shell history exists (vim, htop, less) sees nothing on wheel scroll while the compositor slides stale scrollback over the alt screen — wrong behavior a user hits daily.
- **`src/compositor/ServerMouse.zig:167`** — `duration_ms * 60 / 1000` multiplies in u32; an MCP-supplied duration_ms > 71,582,788 overflows and panics (Debug/ReleaseSafe), aborting the compositor and killing the session
  - curvedPath computes `const samples: u32 = @max(8, @min(MAX_SAMPLES, duration_ms * 60 / 1000));`. duration_ms is u32 and arrives from the teruwm_mouse_path MCP tool via satU32 (WmMcpTools.zig:66-68, used at WmMcpTools.zig:362), which permits any value up to 4,294,967,295. For duration_ms > maxInt(u32)/60 ≈ 71.6e6, `duration_ms * 60` overflows u32 → integer-overflow panic in Debug/ReleaseSafe builds → the whole compositor (the display server) aborts. The very next line (168) handles the identical computation correctly by widening first: `(@as(u64, duration_ms) * 1_000_000)`. In ReleaseFast the wrap is benign (samples clamps to [8,240]), which makes this a safety-mode-only session-killer reachable from one JSON argument on the local MCP socket.
- **`src/compositor/ServerWindow.zig:242`** — nodeAtPoint has no fullscreen awareness — clicking inside a fullscreen window can silently move keyboard focus to a hidden window
  - nodeAtPoint (ServerWindow.zig:242-265) hit-tests registry rects filtered only by workspace, never by visibility or fullscreen_node. During fullscreen, recomputeVisibility hides all other nodes but their tile rects in the registry are unchanged, and the fullscreen node's rect (0,0,W,H) contains every point. The scan overwrites best_tiled in ascending slot order, so any hidden window with a higher slot index than the fullscreen node wins the hit-test wherever its stale tile rect lies. Both consumers are live: ServerCursor.zig:421 forwardAndFocus calls it on every button press and will focusTerminalByNode/focusView the HIDDEN window (seat keyboard leaves the fullscreen surface; subsequent typing feeds an invisible window; terminalMousePress even starts a drag-selection on the invisible pane), and ServerCursor.zig:329 tryBeginFloatDrag can Super+drag-float a hidden window. Repro: open two windows, Mod+F the first-opened one, click in the region of the second's tile — focus silently leaves the fullscreen window. ServerCursor has zero fullscreen guards (verified by grep).
- **`src/compositor/ServerRestart.zig:279`** — active_ws byte from the restart blob is never validated; a value >= 10 causes workspaces[active_ws] index-OOB panics after the seat was released (frozen TTY)
  - restoreSession reads `const active_ws = blob[pos]` (line 279) with no clamp. The file painstakingly clamps every OTHER blob field against exactly this hazard class: the layout byte (lines 285-292), raw_ws=0xFF (lines 331-340), rows/cols=0 (lines 343-347) — each comment citing 'aborting the re-exec'd binary AFTER the seat was released (frozen TTY)'. But active_ws itself flows unclamped into: (a) line 340 `ws = active_ws` when raw_ws is out of range, then TerminalPane.createRestored → `server.layout_engine.workspaces[ws].addNode` (TerminalPane.zig:331) — workspaces is [10]Workspace, so ws>=10 is an index-out-of-bounds panic; (b) parkRestoredScratchpad line 675 `&server.layout_engine.workspaces[active_ws]`; (c) line 380 setWorkspaceVisibility(active_ws, true) → ServerWindow.zig:43 `self.layout_engine.workspaces[ws]`. switchWorkspace (LayoutEngine.zig:95-99) clamps, but it runs at line 379 — after createRestored already indexed, and before the unclamped calls at 380-381. A corrupt or version-skewed teruwm-restart.bin with one bad byte at offset 2 panics the re-exec'd binary before it can take the display — identical consequence to the shipped workspaces[255] scratchpad-restore crash (fixed 00c5dbe) that this file's own comments memorialize.
- **`src/compositor/Bar.zig:349`** — fork() failure check `if (pid < 0)` is always false because std.os.linux.fork() returns usize; a failed fork then panics at the @intCast into Reaper.track
  - /usr/lib/zig/std/os/linux.zig:819 declares `pub fn fork() usize` (raw syscall return; failure is -errno encoded as a huge unsigned value). At Bar.zig:348-349 `const pid = std.os.linux.fork(); if (pid < 0)` compiles but the branch is unreachable. On fork failure (EAGAIN under pid-limit/cgroup pressure, ENOMEM), pid is e.g. 0xFFFF...F5, so the code takes the parent path and hits `Reaper.track(@intCast(pid))` at Bar.zig:389 where the target type is posix.pid_t (i32, per Reaper.zig:40). In Debug/ReleaseSafe (which is what `make release` ships, per CLAUDE.md) the @intCast is a safety panic — the whole compositor aborts, losing the session — triggered by transient resource exhaustion during a routine bar-widget refresh. In ReleaseFast it is unchecked UB (practically truncates to a negative pid that Reaper.track rejects, and the writer-less pipe self-heals via EOF). The in-repo correct pattern exists at src/compat.zig:423-425 (`posixFork() isize` via @bitCast). Note src/compositor/ServerProcess.zig:12 has the same unsigned-fork pattern (outside this review's file set).
- **`src/compositor/XdgView.zig:387`** — Client-initiated unmap of the focused xdg/xwayland window never reconciles keyboard focus — focus is left null until the user manually clicks or presses a focus keybind
  - XdgView.handleUnmap (XdgView.zig:342-390) and XwaylandView.unmapView (XwaylandView.zig:211-285) call clearFocusRefs (nulls focused_view/focused_xwayland), remove the node, and arrangeworkspace — but never call updateFocusedTerminal. Verified: ServerLayout.arrangeWorkspace (ServerLayout.zig:58-141) does no focus reconciliation, and there is no focus-follows-mouse (ServerCursor only focuses on button press, forwardAndFocus Phase D). The terminal-pane close path DOES reconcile (ServerWindow.zig:328 and :443 call updateFocusedTerminal, with ServerWindow.zig:433-443 explicitly documenting 'leave keyboard focus null until the user manually refocuses' as the bug being fixed there). But closeNode's XDG branch (ServerWindow.zig:340-351) only sends send_close, so even Win+X on an xdg window funnels into handleUnmap and ends with dead keyboard focus. Closing a browser via its own close button leaves typing going nowhere until a click.
- **`src/compositor/ServerCursor.zig:393`** — tryBeginBorderDrag never checks nodes.floating — a visible floating pane/scratchpad's right edge is a click-hijack strip that starts a master-ratio drag
  - The loop over terminal_panes (lines 385-402) filters on `workspace[slot] != cur_ws` (the #45 fix) but not on `server.nodes.floating[slot]`. A shown scratchpad is exactly that state: ServerScratchpad.zig:298 sets floating[slot]=true with workspace = active ws; tryBeginFloatDrag (line 347) creates the same state. With >=2 tiled nodes on the workspace (line 378 gate), any non-Super button press landing in the 10px strip around a floating pane's right edge (border_drag_insensitive_px=2 inside + border_drag_zone_px=8 outside, WmConfig.zig:338-339) is hijacked into cursor_mode=.border_drag: the click never reaches the pane and the master ratio gets dragged instead. This is the same dead-click-strip bug class as the fixed #45, which the comment at lines 388-392 documents — the fix just stopped one predicate short.
- **`src/compositor/ServerInput.zig:466`** — mods.ralt is never set in teruwm, so the hardcoded Alt+RAlt+1..9 scratchpad chord and every user 'ralt+...' keybind are silently dead in the compositor
  - handleKey builds mods from only ALT/SHIFT/CTRL/LOGO via xkb_state_mod_name_is_active (ServerInput.zig:410-414); no code in src/compositor/ ever sets mods.ralt (grep confirms line 466 is the only ralt reference in the compositor). Therefore `if (mods.alt and mods.ralt ...)` at line 466 can never be true, and since Keybinds.Mods.eql is an exact u8 bitcast compare (Keybinds.zig:29-31), any config binding parsed with the supported 'ralt' token (Keybinds.zig:513-515) can never match in teruwm either. docs/KEYBINDINGS.md:97 documents 'the hardcoded Alt+RAlt+1..9 chord toggles the numbered compat pads pad1..pad9' as a live teruwm feature. In standalone teru, ralt comes from the platform layer (KeyHandler.zig:183-201 platformMods(modifiers, ralt)) — the compositor path simply never grew the equivalent Alt_R tracking (xkb modifier names cannot distinguish left vs right Alt; it needs per-keyboard Alt_R press/release state or keycode inspection). What the user actually gets when pressing the documented chord: mods={alt}, key='1'..'9' misses the keybind lookup, then with a focused terminal the Alt path at lines 170-176 writes ESC+digit into the PTY — garbage into the shell instead of a scratchpad toggle.
- **`src/compositor/ServerWindow.zig:543`** — moveNodeToWorkspace's invisible-target focus fixup only covers focused_terminal — moving a focused XDG window off-screen leaves keyboard focus on the hidden client
  - Lines 519 and 543-551 drop focus only when the moved node was the focused TERMINAL and the target workspace is shown nowhere. But the primary keybind path (ServerInput.zig:563-570, Mod+Shift+N) explicitly prefers focused_view (the fix comment there says so), so the common case is moving a focused xdg toplevel. After the move, recomputeVisibility hides it, but focused_view stays set, the seat keyboard focus stays on the now-invisible surface, and isSlotFocused (ServerLayout.zig:145) keeps painting it as focused — every subsequent keystroke silently feeds an off-screen browser, which is exactly the failure mode the comment at lines 516-518 says this guard exists to prevent. focused_xwayland is likewise never considered.
- **`src/compositor/TerminalPane.zig:589`** — repaintBorderOnly splices live-grid content into a scrolled-back viewport and strips selection highlight on the cursor rows
  - The focus-flip fall-through path (lines 586-589) does `grid.markRowDirty(grid.cursor_row)` then `self.renderer.renderDirty(grid)` — which repaints the live-grid rows [min(last_cursor_row,cursor_row)..max+1) into the framebuffer with NO selection overlay and NO applyScrollOverlay. Two confirmed consequences: (1) If the pane is scrolled into scrollback (framebuffer currently shows the history overlay painted by the last renderIfDirty), a focus flip paints one or more LIVE grid rows into the middle of the history view, commits full-buffer damage, and the corruption persists until the pane next repaints (new PTY output or user scroll) — the grid is clean after renderDirty so renderIfDirty won't fix it. Reproduce: scroll pane A back, click into pane B → ServerCursor.zig:497 `prev.repaintBorderOnly()`. (2) If a completed selection is visible (selection persists after release by design, ServerCursor.zig:609-624) and overlaps the cursor rows, the highlight is visibly stripped from those rows on every focus flip because renderDirty passes sel=null (contrast renderIfDirty line 518 which passes &self.selection). Note the border-removed branch (line 575-579) already routes through render(), which applies both overlays correctly — only the fall-through partial path is wrong.
- **`src/compositor/Server.zig:1427`** — spawnTerminal violates the documented focus XOR invariant: it nulls focused_view but not focused_xwayland, so Win+X after spawning a terminal over an X11 window closes the X11 app instead of the focused terminal
  - Server.zig:167-170 documents 'Exactly one of focused_terminal, focused_view, focused_xwayland is non-null at any time — focus helpers null the other two.' spawnTerminal (lines 1427-1428) sets focused_terminal and nulls focused_view only. If an Xwayland window (Emacs, Steam, GIMP) was focused, focused_xwayland stays set: (1) closeFocused (ServerWindow.zig:357-370) checks focused_xwayland BEFORE focused_terminal, so window_close/pane_close (Win+X) sends WM_DELETE_WINDOW to the X11 app instead of closing the just-spawned, visibly-focused terminal; (2) isSlotFocused (ServerLayout.zig:153) keeps painting the X11 window's border as focused (two 'focused' windows on screen); (3) the previous xwayland surface never gets wlr_xwayland_surface_activate(false) and the previous XDG toplevel is never deactivated either, so the client keeps drawing active decorations. Reachable from the spawn_terminal keybind (ServerInput.zig:576, 770) and the MCP tool (WmMcpTools.zig:553). Compare ServerFocus.zig:55-56, which deactivates and nulls focused_xwayland on every proper focus transition.

### tiling
- **`src/tiling/Workspace.zig:136`** — removeNode does not decrement active_index when the removed pane sits before it — focus silently jumps to the next pane when a background pane exits
  - removeNode (129-157) does orderedRemove(i), shifting all later ids left, then only clamps active_index when it is out of range (137-141). If the removed index i < active_index, active_index now names the pane AFTER the one the user had focused. Concrete repro in flat mode: panes [A,B,C], focus on B (active_index=1); A's background shell exits → daemon reapDeadPanes calls ws.removeNode(dead_id) (src/server/daemon.zig:627) → node_ids=[B,C], active_index still 1 → getActiveNodeId()/getActivePaneMut (src/core/Multiplexer.zig:92-95) now return C, and subsequent keystrokes go to pane C's shell. Same trigger via Multiplexer.closePane on a non-active pane (MCP close with explicit pane_id, src/core/Multiplexer.zig:280). This also desyncs active_index from active_node (which correctly still holds B per lines 146-148), violating the A1 single-source invariant documented at 277-288 — the exact dual-source-of-truth family behind the project's historical wrong-pane focus bugs.
- **`src/tiling/Workspace.zig:341`** — Flat-to-tree conversion in addNodeSplit drops every pre-existing pane except node_ids[0] — remaining panes vanish from the layout while their shells keep running
  - When split_root == null and node_ids.items.len >= 2 (lines 339-360), addNodeSplit seeds the new tree with exactly TWO leaves: the first non-new id found in the flat list (the `existing_id` block at 343-348 always picks node_ids[0]) and the new pane. Any other panes stay in node_ids but never enter the tree. Once split_root != null, layout comes exclusively from the tree: LayoutEngine.calculateWith (src/tiling/LayoutEngine.zig:72-74) calls calculateFromTree, and Multiplexer render/rect paths use getTreePaneIds (src/core/Multiplexer.zig:117-123) — panes absent from the tree get no rect and disappear from screen. Reachable today: teru windowed mode with 2+ panes in a flat layout, press split_vertical/split_horizontal (src/modes/windowed.zig:963,1095), or call the MCP split tool (src/agent/McpServerTools.zig:437) on a workspace with 2+ flat panes. Secondary defect in the same block: the split targets node_ids[0], not the focused pane (active_index/active_node is ignored), so even the 2-pane case splits the wrong pane when the second pane is focused.
- **`src/tiling/Workspace.zig:435`** — Split-node slots are never reclaimed on removal — split/close churn exhausts the 64-slot pool, after which addNodeSplit silently no-ops and new panes become invisible
  - allocSplitNode (308-314) is bump-only against the fixed split_nodes[64] array. removeLeafRecursive (435-456) collapses a split by copying the sibling node over the parent slot, orphaning 2 slots per removal and never decrementing split_node_count. The count only resets when the LAST tree pane is removed (line 412) or when callers explicitly clear the tree on layout switch (src/core/Multiplexer.zig:375-377, 426-428, 445-447). So a user who keeps a split tree alive and repeatedly splits+closes helper panes (each cycle: +2 slots allocated at lines 378-379, 0 reclaimed) exhausts the pool after ~30 cycles in a long-lived daemon session. From then on every allocSplitNode returns null and addNodeSplit bails via bare `orelse return` (349, 350, 356, 362, 375, 378, 379) with NO log and NO error — but the pane was already appended to node_ids (line 328), so it exists in the flat list, is absent from the tree, and gets no rect (invisible live shell). At the 63-slot boundary the paired allocation at 378-379 half-succeeds, leaking yet another slot. This is the same tree/flat 'ghost pane' divergence class LayoutEngine.moveNodeToWorkspace's comment (src/tiling/LayoutEngine.zig:110-115) documents as a shipped bug.

### tools
- **`src/tools/teruwmctl/main.zig:107`** _(→medium)_ — Running bare `teruwmctl` prints nothing: usage text (1364 bytes) overflows errFmt's 1024-byte buffer and the error is swallowed
  - errFmt (line 49-53) formats into a fixed [1024]u8 via std.fmt.bufPrint and does `catch return` — on error.NoSpaceLeft it prints NOTHING, not even a truncated message. The usage string literal (lines 55-97) measures 1364 bytes, so the no-args path at line 107 (`errFmt("{s}", .{usage})`) silently emits zero output and exits 3. A user typing `teruwmctl` to discover the tool gets dead silence. The same swallow-on-overflow bites line 655 (`couldn't parse socket path from: {s}` with `inner` up to ~4KB): the entire diagnostic vanishes. Note the -h path (line 113) works because it uses `out(usage)` directly, bypassing the buffer.

### windows(deferred)
- **`src/platform/windows/platform.zig:333`** _(→medium)_ — Window size is treated as client size: no AdjustWindowRectEx, and the corrective initial WM_SIZE is dropped because GWLP_USERDATA is set only after CreateWindowExW returns
  - CreateWindowExW (line 333) receives width/height as the OUTER window size (title bar + borders included), but state.width/height (line 319-324) record the same values as if they were the client area. The WM_SIZE that Windows sends during creation, carrying the true client size, is discarded because wndProc bails at line 506 while GWLP_USERDATA is still 0 — it is only set at line 355, after CreateWindowExW returns. state.width/height therefore stay too large; windowed.zig:200 computes the grid from getSize(), so the bottom rows/right columns of the terminal are rendered outside the real client area and clipped by GDI until the first resize (which then crashes per the WM_SIZE finding). setSize (lines 451-456) repeats the confusion: SetWindowPos synchronously sends WM_SIZE, whose handler writes the correct client dims into state, and then lines 454-455 overwrite them with the requested outer dims.
- **`src/platform/windows/platform.zig:680`** — getKeyModifiers bit layout (shift=0x01, ctrl=0x02, alt=0x04, super=0x08) contradicts the shared WindowsKeycodes masks and windowed.zig's hardcoded Alt bit — Shift is decoded as Alt and Alt as Shift
  - getKeyModifiers (platform.zig:677-685) emits VK_SHIFT→0x01, VK_CONTROL→0x02, VK_MENU→0x04, VK_WIN→0x08. But src/platform/types.zig:84-88 declares WindowsKeycodes as ALT_MASK=0x01, CTRL_MASK=0x02, SHIFT_MASK=0x04 (the Win32 RegisterHotKey MOD_* values), and src/core/KeyHandler.zig:185-188 decodes event modifiers with exactly those masks. On Windows every Shift press therefore sets m.alt=true — typing a capital letter can trigger Alt-bound pane-management shortcuts — and a real Alt press sets m.shift. Only Ctrl (0x02) agrees by coincidence. Additionally src/modes/windowed.zig:866, :892, and :1105 hardcode `key.modifiers & 8` (X11 Mod1Mask) for Alt detection, which on Windows matches the SUPER bit from getKeyModifiers, so the Win key is treated as Alt there. Three incompatible conventions across three files.

## Medium findings (surviving, both runs)

### agent/mcp
- `src/agent/forward.zig:57` — Socket discovery takes the first readdir match with no liveness check, and forwardRequest never falls back to the next candidate
- `src/agent/in_band.zig:75` — (Adjacent file, found while verifying protocol.zig's query path) 7 of the 12 in-band allowlisted tools do not exist in teru's MCP tool table — the advertised agent-metadata and widget surface can never work in-band
- `src/agent/protocol.zig:76` — scanForAgentSequences (about 100 lines plus 10 tests) has zero production callers and duplicates VtParser's OSC 9999 handling with divergent, buggier semantics
- `src/agent/forward.zig:103` — Write return counts are discarded — a short write silently truncates the forwarded HTTP request
- `src/agent/forward.zig:107` — Responses larger than the 64 KiB stack buffer are silently truncated and returned as if complete
- `src/agent/HookListener.zig:113` — Content-Length is accumulated from an untrusted HTTP header with no overflow guard, panicking in safe builds.
- `src/agent/HookListener.zig:135` — HTTP requests larger than the fixed 8192-byte buffer are truncated, then silently dropped with no HTTP response.
- `src/agent/HookListener.zig:150` — poll() blocks the single-threaded main loop for up to 200 ms waiting on a stalled hook client.
- `src/agent/PaneBackend.zig:188` — context_exited effectively always reports exit_code 0 — the real waitpid status is discarded, so a crashed agent is indistinguishable from a successful one
- `src/agent/McpServer.zig:198` — emitEvent hardcodes EAGAIN=11, which is wrong on macOS (35), so backpressure on darwin kills the event subscriber — resurrecting the exact bug the comment claims was fixed; EINTR and partial writes are also mishandled
- `src/agent/PaneBackend.zig:234` — spawn ignores the documented env parameter entirely and only accepts argv as a JSON string — a spec-shaped argv array yields '-32602 Missing argv'
- `src/agent/McpServer.zig:260` — Read-only blocklist omits teru_screenshot and teru_session_save — both perform filesystem writes (screenshot to an arbitrary caller-supplied path) while TERU_MCP_READONLY is active
- `src/agent/McpFramework.zig:264` — line_json response path assumes dispatch's return slice points into resp_buf; static-literal fallback returns make it send uninitialized stack bytes to the client
- `src/agent/McpServerTools.zig:349` — toolGetGraph emits malformed JSON when the 64KB response buffer fills: mid-entry `catch break` leaves an unterminated object; toolListPanes similarly leaves a trailing comma
- `src/agent/PaneBackend.zig:378` — handleCapture silently drops every non-ASCII glyph (cp >= 127) with no placeholder, shifting column alignment and erasing all Unicode content from captures
- `src/agent/McpServer.zig:384` — dispatch_table is paired to McpDispatch.tools purely by index; the comment claims 'a mismatch is a compile-time array-length error' but only length mismatches are caught — a transposition silently routes tools to the wrong handlers
- `src/agent/McpServerTools.zig:512` — Partial PTY writes silently dropped in toolSendInput / toolBroadcast / toolSendKeys — short write on a full O_NONBLOCK master loses the tail of the input yet reports success
- `src/agent/McpServerTools.zig:669` — toolSetConfig: teru.conf larger than 16KB is silently truncated and the truncated content is atomically renamed over the original — permanent loss of everything past 16KB; rename result also unchecked
- `src/agent/McpServerTools.zig:954` — toolWaitFor: hand-rolled JSON escape only handles quote/backslash, and `ch < 128` admits char==0 wide-glyph trailers — a matched line containing wide chars embeds raw NUL bytes in the JSON response

### config
- `src/config/ConfigWatcher.zig:131` — macOS kqueue watcher dies silently after the first atomic-save: it watches the opened file vnode with only NOTE.WRITE|NOTE.ATTRIB, so rename-replace editors orphan the watch
- `src/config/SessionDef.zig:182` — free-before-dupe on duplicate keys leaves a dangling pointer that the parse errdefer double-frees on OOM
- `src/config/Config.zig:247` — External theme files override explicit user color keys, inverting the precedence that builtin themes honor
- `src/config/SessionDef.zig:257` — save/restore round trip silently drops panes: saveFromLive writes unbounded pane numbers that parse caps at 16, and buffer overflow is swallowed with bare `catch break`
- `src/config/Keybinds.zig:585` — The entire keycode-binding feature is dead: lookupKeycode has zero callers, and lookup() explicitly skips is_keycode bindings — a `keycode:44 = action` config line (advertised in parseLine's doc comment) is accepted, stored, and can never fire
- `src/config/Config.zig:603` — `mod =` after included keybinds silently wipes all user keybindings (even when the mod value is invalid)
- `src/config/Keybinds.zig:792` — The scroll-mode and search-mode binding tables (defaults lines 792-811) and the .scroll/.search/.locked modes are never consulted — no dispatch path calls lookup with those modes, so [keybinds.scroll]/[keybinds.search] config sections parse successfully and silently do nothing

### core
- `src/core/Multiplexer.zig:386` — focusMaster writes only ws.active_index, so the focus-master keybind is a silent no-op in split-tree workspaces and desyncs the two focus fields — the documented dual-source-of-truth bug pattern
- `src/core/VtParser.zig:450` — decodeUtf8 accepts overlong encodings — an overlong NUL (0xC0 0x80) decodes to codepoint 0 and forges the wide-glyph-trailer sentinel in the grid
- `src/core/Multiplexer.zig:468` — resizePanePtys ignores the agent status bar height that renderAllWithSelection and getActivePaneRect both subtract — with agents active, the bottom row(s) of panes exist in the PTY but are clipped out of the render
- `src/core/VtParser.zig:520` — RIS (ESC c) reset is incomplete: alt screen, mouse tracking, bracketed paste, DECAWM, DECLRMM margins, app cursor keys, and sync_output all survive a full reset
- `src/core/Multiplexer.zig:594` — renderAllWithSelection does a full-framebuffer memset plus full repaint of every cell of every pane on any dirty frame — dirty-row tracking exists but is never consulted, so the cursor-blink 'mark only the cursor row dirty' optimization is a no-op
- `src/core/Multiplexer.zig:594` — renderAllWithSelection memsets the entire framebuffer to bg every frame, then renderPaneIntoRect re-fills every cell's bg — pane pixels are written twice
- `src/core/Grid.zig:617` — scrollUpN captures to scrollback for DECSTBM-bounded regions: missing `scroll_top == 0` guard the comment claims exists
- `src/core/Grid.zig:658` — scrollback line encoder duplicates dumpRowAnsi's SGR-delta logic and has diverged: orphan trailers/control chars shift columns, and the fixed 4096-byte buffer truncates heavily-styled rows
- `src/core/VtParser.zig:681` — Any CSI final with a '$' intermediate is forwarded to plain dispatchCsi, so DECCARA (CSI ... $ r) is executed as DECSTBM and DECRQTSR (CSI Ps $ u) as restore-cursor
- `src/core/ViMode.zig:774` — All 15 ViMode inline tests are dead — ViMode is the one core module missing from the src/lib.zig test root — and when actually compiled, the 'half-page movement' test FAILS (expected 124, found 123)
- `src/core/Grid.zig:810` — ED mode 3 (`ESC[3J`) is documented as clearing scrollback but is implemented identically to mode 2 — scrollback is never cleared
- `src/core/VtParser.zig:911` — CUU/CUD/CNL/CPL unconditionally clamp to the scroll region, teleporting a cursor that starts OUTSIDE the region into it (wrong direction of movement)

### graph
- `src/graph/ProcessGraph.zig:98` — The 1024-node cap is ineffective against running nodes — hostile pane output can grow the graph (and its duped strings) without bound

### input
- `src/input/TuiInput.zig:172` — The prefix-byte check precedes the prefix_active check, so a literal prefix byte can never be sent to the pane — in nested mode Ctrl+A (readline beginning-of-line) is permanently unusable

### macos
- `src/platform/macos/platform.zig:213` — setOpacity early-returns when opacity >= 1.0, so a live config reload can never restore the window to fully opaque
- `src/platform/macos/platform.zig:273` — key.modifiers carries raw NSEvent modifierFlags but consumers mask it with X11-style bit values, which on macOS collide with NX device-dependent bits — Ctrl/Shift/Alt chord detection misreads modifiers.
- `src/platform/macos/keyboard.zig:359` — Caps Lock is treated as Shift for every key, not just letters: with Caps Lock on, digit and punctuation keys would produce their shifted symbols ('1' -> '!', '-' -> '_').
- `src/platform/macos/platform.zig:366` — putFramebuffer allocates and destroys NSBitmapImageRep + NSImage + NSString every frame and draws through a deprecated lockFocus/flushWindow path

### modes
- `src/modes/tui.zig:354` — SIGWINCH resize sends full terminal rows to the daemon while the attach path sends rows-1 (status bar) — panes transiently resized one row too tall, shells get double SIGWINCH per resize
- `src/modes/windowed.zig:677` — The .resize handler never forces a repaint, so a resize that doesn't change any pane's cell dimensions leaves the window blank/stale (Wayland has no expose to self-heal)
- `src/modes/windowed.zig:1446` — Live config reload applies padding and show_status_bar to the renderer but never recomputes dependent geometry, desyncing grid, PTY sizes, and mouse hit-testing
- `src/modes/windowed.zig:1496` — Cursor blink triggers a full-screen repaint every 530 ms while focused; the comment claiming the cursor-row-only optimization is false
- `src/modes/windowed.zig:1529` — Windowed render loop has no frame-rate cap on the actual render; the 60fps cap only shrinks the idle poll timeout and is bypassed under continuous PTY output
- `src/modes/windowed.zig:1529` — Rendering is not throttled to the frame cap when the loop is busy: one full-screen repaint per ≤8KB of PTY output
- `src/modes/windowed.zig:1531` — Visual bell is a no-op: the framebuffer inversion is fully overwritten by the render pass before it is ever presented
- `src/modes/windowed.zig:1675` — PaneBackend's listening and client fds are absent from waitForInput's poll set — every protocol request waits for the next loop wake, up to the 500 ms idle cap

### other
- `tools/fix-crt.sh:1` — The GCC-15 .sframe CRT workaround is dead on Zig dev.1417 — the linker gap is closed, yet every Makefile target still runs it and forces `--libc`.
- `src/main.zig:73` — `-f, --fresh` is advertised in --help but never parsed — the flag is a silent no-op
- `Makefile:88` — `make run-wm` passes a spurious bare `--libc`, breaking the target in both CRT-cache states.

### persist
- `src/persist/Scrollback.zig:137` — The whole keyframe reconstruction machinery (getLine/getRange/findKeyframe/decodeKeyframe) has zero production callers, yet pushLine still pays for encodeKeyframe — a two-pass full-grid scan + allocation every 100 scrolled lines on the PTY-drain path
- `src/persist/Session.zig:253` — deserialize validates node count against corruption but reads per-workspace pane_count (u16) completely unvalidated, and downstream restore spawns that many shells
- `src/persist/Scrollback.zig:483` — decodeKeyframe restores scroll_top/scroll_bottom unclamped while clamping only the cursor — a keyframe from a taller grid decoded into a shorter grid leaves scroll_bottom >= rows, and the next replayed scroll indexes cells out of bounds

### platform-linux
- `src/platform/linux/keyboard.zig:136` — On X11 the layout group is never synced from the server, and resetState on focus-in silently resets multi-layout users to layout 0
- `src/platform/linux/keyboard.zig:137` — resetState unrefs the current xkb_state before creating its replacement; on failure self.state dangles and every subsequent key event is a use-after-free
- `src/platform/linux/x11.zig:495` — Non-SHM fallback xcb_put_image declares width=blit_w but sends data with stride fb_width and data_len computed from fb_width — malformed request whenever the defensive clamp engages
- `src/platform/linux/wayland.zig:652` — Full-framebuffer memcpy + full-surface damage on every commit, discarding the project's dirty-row tracking
- `src/platform/linux/wayland.zig:661` — Single SHM buffer with no wl_buffer.release listener — teru writes into the buffer while the compositor may still be reading it, and destroys an in-use buffer on resize
- `src/platform/linux/wayland.zig:748` — ARGB8888 buffer with no wl_surface.set_opaque_region although Wayland pixels are always fully opaque — forces per-frame compositor alpha-blending and defeats occlusion culling

### pty
- `src/pty/PosixPty.zig:111` — Unbounded @memcpy of shell path into 4096-byte stack buffer in the forked child — no length check, unlike the cwd copy 14 lines above
- `src/pty/PosixPty.zig:139` — write() conflates EAGAIN and EINTR with fatal errors (all become error.WriteFailed), asymmetric with read(); callers drop typed input or busy-poll on real errors
- `src/pty/WinPty.zig:262` — buildCommandLineFromArgv joins argv with spaces and no Windows quoting, and both command-line builders zero-extend UTF-8 bytes to UTF-16 (mojibake)

### render
- `src/render/Compositor.zig:56` — Windowed/multiplexer render path repaints every cell of every pane on every dirty frame, ignoring grid.dirty_row_min/max
- `src/render/BarRenderer.zig:147` — refreshCachedData does synchronous ACPI-EC-backed sysfs reads on the single-threaded wl event loop, with battery and watts TTLs phase-locked so up to 4 EC reads land in one frame callback
- `src/render/TuiRenderer.zig:161` — Discarded sendMessage failure plus unconditional size-cache update means a dropped pane resize is never retried — daemon-side PTY winsize stays permanently stale
- `src/render/Ui.zig:277` — Scroll indicator arrow (U+2191) in the status bar never renders — its three UTF-8 bytes are fed byte-wise to blitCharAt, which rejects anything >= 127
- `src/render/software.zig:300` — `@as(u16, @intCast(scroll_offset + row))` overflows when scrollback exceeds 65535 lines, panicking safe builds / corrupting the selection highlight in release
- `src/render/software.zig:313` — Bold-is-bright is applied after inverse/dim/hidden/selection resolution, making bold+reverse text near-invisible and un-hiding/un-dimming bold text
- `src/render/Ui.zig:319` — hitTestStatusBar replays the workspace-tab layout without renderTextStatusBar's fb_w/2 name cap, so long workspace names desync click regions and clicks focus the wrong workspace
- `src/render/BarRenderer.zig:411` — measureWidgets hardcodes the clock at 5 cells while the clock format is user-configurable, clipping longer formats off the right edge
- `src/render/software.zig:441` — resize() frees the old framebuffer before allocating the new one — on alloc failure the renderer keeps a dangling pointer, and the caller swallows the error and keeps rendering into freed memory
- `src/render/software.zig:441` — SoftwareRenderer.resize frees the old framebuffer before allocating the new one; on OOM the swallowed error leaves self.framebuffer dangling (found via the windowed.zig:623 call site)
- `src/render/FontAtlas.zig:469` — No max_font_size clamp: extreme zoom-in overflows the u16 @intCast of atlas_y (ReleaseSafe panic) and drives O(size^2) allocations
- `src/render/FontSynth.zig:509` — setPixelCell only rejects negative coordinates, so drawThickLine with extra=2 (heavy ✔ U+2714 / ✘ U+2718 synthesis) bleeds pixels into the horizontally adjacent atlas slot, contradicting its own no-bleed doc comment

### server
- `src/server/protocol.zig:152` — recvMessage consumes a partial 5-byte header and returns null, permanently desyncing the wire — the same bug class explicitly fixed for the body path but left in the header path
- `src/server/daemon.zig:211` — Stale fds[1] revents + recvMessage's EAGAIN/EOF conflation can instantly disconnect a freshly-accepted client
- `src/server/protocol.zig:247` — writeAll/recvMessage blocking poll loops let a slow-drip client wedge the single-threaded daemon indefinitely

### teruwm
- `src/compositor/ServerWindow.zig:91` — toggleFloat re-tiles into layout_engine.active_workspace instead of the node's own workspace, and swallows the addNode error
- `src/compositor/Bar.zig:92` — Bar.create uses allocator.create + field-by-field init, so the declaration-default fields last_top_sig, last_bottom_sig, dirty, and cache_dirty are never initialized (undefined memory)
- `src/compositor/ServerWindow.zig:117` — toggleFullscreen (Mod+F) and toggleFloat ignore focused_xwayland — silent no-op on every X11 window
- `src/compositor/ServerRestart.zig:128` — Restart blob (full terminal display memory + up to 1 MiB scrollback per pane) falls back to a predictable, symlink-followable, world-readable path in /tmp when XDG_RUNTIME_DIR is unset
- `src/compositor/ServerScreenshot.zig:130` — Full-output screenshot composites, reads back every client texture, and zlib-encodes the PNG entirely on the wl_event_loop thread, blocking libinput and frame callbacks for the whole capture.
- `src/compositor/Node.zig:135` — addSurface/addTerminal swallow by_id.put failure with `catch {}` — on failure the slot leaks permanently and the window becomes unmanageable
- `src/compositor/ServerCursor.zig:144` — Vertical wheel routing keys off keyboard focus (focused_terminal), not the pane under the cursor — hover-scroll behaves incoherently
- `src/compositor/ServerMouse.zig:167` — `duration_ms * 60` is evaluated in u32 and overflows for durations above ~71.6M ms, which panics in Debug/ReleaseSafe builds and is illegal behavior in the shipped ReleaseFast build.
- `src/compositor/XwaylandView.zig:172` — Tiled X11 map path never emits the window_mapped MCP event that the XDG path emits — MCP event subscribers miss every Xwayland window
- `src/compositor/ServerWindow.zig:174` — exitFullscreen never restores a floating node's pre-fullscreen geometry — the window stays full-output at (0,0) forever
- `src/compositor/ServerScreenshot.zig:178` — External-client capture does synchronous GPU texture readback per window on the single-threaded wl event loop; repeated MCP screenshots of a full-screen browser stall real input
- `src/compositor/ServerFocus.zig:232` — unfocused_opacity fade only updated by updateFocusedTerminal; cycleFocusAll / focusView / focusXwaylandSurface never re-apply it, so the fade tracks the wrong pane
- `src/compositor/Bar.zig:240` — While the launcher palette is open, Bar.render repaints it with full damage on every frame callback, creating a self-sustaining full-refresh-rate repaint loop
- `src/compositor/ServerInput.zig:267` — refreshActiveKeymap's module-global cache keys on a possibly-freed keymap pointer (ABA), so the bar's layout label can stay stale after a config reload changes the layout
- `src/compositor/Output.zig:278` — Output disconnect reassigns focused_output but never syncs layout_engine.active_workspace, desyncing the two 'current workspace' authorities on multi-monitor hotplug.
- `src/compositor/ServerWindow.zig:306` — closeNode's terminal branch duplicates handleTerminalExit's teardown and has already drifted: no startup-flag reset, no active_node re-seat
- `src/compositor/ServerCursor.zig:339` — tryBeginFloatDrag mutates layout (auto-floats the pane) before checking the button, so Super+middle-click permanently floats a tiled window and starts no drag
- `src/compositor/ServerRestart.zig:354` — `Pane.initWithPty(...) catch continue` leaks the inherited PTY master fd and orphans its shell on alloc failure — the exact leak the createRestored failure path three lines below was patched for
- `src/compositor/WmMcpTools.zig:364` — Literal `"humanize":false` string match fails on whitespace — the exact antipattern extractNestedJsonBool's own docs warn against
- `src/compositor/ServerWindow.zig:436` — handleTerminalExit re-arranges only the ACTIVE workspace, not the exited pane's workspace
- `src/compositor/TerminalPane.zig:449` — sync_flushed is consumed and discarded when the fps cap defers the paint, causing spurious 150 ms DEC-2026 holds
- `src/compositor/Bar.zig:451` — execReadable stores new exec-widget output but never marks the bar dirty, so {exec:N:cmd} results can stay invisible for up to 60 s — the comments at lines 253 and 289 claiming it "marks bar dirty" are false
- `src/compositor/TerminalPane.zig:472` — A lingering completed selection permanently disables the max_render_fps throttle for the pane
- `vendor/miozu-wlr-glue.c:507` — miozu_pixel_buffer_resize frees the backing store and mutates base.width/height of a live wlr_buffer in place, with an undocumented 'must detach from scene first' contract that one caller already violates
- `src/compositor/Bar.zig:550` — barSignature hashes only the push-widget used-mask, so updating an existing push widget's text via MCP does not repaint the bar for up to 60 s
- `src/compositor/XdgView.zig:580` — handleRequestMove/handleRequestResize ignore the event serial — any client can start an interactive move/resize grab with no button held, un-tiling itself and making the window track the cursor until the next click
- `src/compositor/TerminalPane.zig:623` — writeInput silently drops bytes on partial or failed PTY writes
- `src/compositor/WmConfig.zig:699` — Hex/ARGB color parsing is implemented three times with divergent acceptance rules across the two config files; the same color syntax is valid in one file and silently dropped in the other.
- `src/compositor/WmMcpTools.zig:702` — teruwm_set_config border_width mutates config but never re-arranges or schedules a frame — no visible effect
- `src/compositor/WmMcpTools.zig:821` — Screenshot tools run synchronous full-pane render + PNG encode + file write (and texture readback) on the single-threaded wl_event_loop
- `src/compositor/ServerCursor.zig:890` — Interactive float move/resize bypasses applyRect, so XWayland clients are never told their new geometry (move) or resized at all (resize)
- `src/compositor/WmConfig.zig:966` — Contradictory comments claim absolute-pixel support for [scratchpad.NAME] rects; a pixel value like `w = 400` silently clamps to 100% of the output
- `src/compositor/WmConfig.zig:1091` — workspace_cwd is parsed, stored, and documented to users, but has zero consumers anywhere in the source tree
- `vendor/miozu-wlr-glue.c:1153` — miozu_output_apply_config applies heads sequentially with no rollback, so a failed multi-head apply leaves earlier heads committed while the client is told 'failed'
- `src/compositor/WmMcpTools.zig:1200` — teruwm_press of a plain printable key or Space on a focused native pane misroutes to the seat — types into a background client or nowhere
- `src/compositor/Server.zig:1431` — spawnTerminal full-repaints and full-damage re-uploads every terminal pane — including panes hidden on other workspaces — on each spawn

### tiling
- `src/tiling/Workspace.zig:164` — focusMaster, swapWithNext/Prev, promoteToMaster, swapWithMaster, and rotateSlaves have no tree branch — in split-tree mode these keybinds are silent no-ops

### tools
- `src/tools/teruwmctl/main.zig:411` — parseIntSafe/parseFloatSafe validate with a grammar wider than JSON, then splice the RAW user string into the request — `switch-workspace 1_0` silently switches to workspace 1
- `src/tools/teruwmctl/main.zig:685` — All stdout writes discard std.c.write's return — short writes/EINTR/EAGAIN silently drop bytes, corrupting the `watch` event stream

### windows(deferred)
- `src/platform/windows/keyboard.zig:132` — Caps Lock toggle state is never tracked, so ToUnicode never sees it — Caps Lock has no effect
- `src/platform/windows/keyboard.zig:205` — Alt+letter translation relies on ToUnicode producing a character while VK_MENU is down in key_state, which Win32 does not do — the ESC-prefix path is effectively unreachable
- `src/platform/windows/platform.zig:226` — GetDC and ReleaseDC are declared extern "gdi32" but are user32.dll exports — the Windows build cannot link
- `src/platform/windows/keyboard.zig:246` — Dead-key handling flushes the dead key it is supposed to preserve, so accent composition (dead ´ + e → é) never works
- `src/platform/windows/platform.zig:274` — Single-slot pending_event is overwritten when one PeekMessageW/DispatchMessageW delivers multiple messages — events (focus, close) can be silently lost
- `src/platform/windows/platform.zig:348` — WindowState allocation leaks if CreateWindowExW fails — no errdefer

## Run-2 reviewer assessments

- **review:agent-hooks**: Scope: read all 394 lines of src/core/UrlDetector.zig, all 388 of src/agent/HookHandler.zig, all 225 of src/agent/HookListener.zig, plus verification against src/server/ipc.zig, src/modes/windowed.zig, src/input/mouse.zig, src/compat.zig, and the installed stdlib (/usr/lib/zig/std/mem.zig, posix.zig, c.zig).

UrlDetector.zig is CLEAN — no reportable findings. Verified: scanRow loop always makes forward progress (col = trimmed_end/end, both strictly > col), so no infinite loop; slice bounds `cells[col..col+8]` are guarded by `col+8 <= cols` with cells.len == cols; the double-fork in openUrl uses a TARGETED `std.c.waitpid(pid,...)` (not waitpid(-1)), so it does not violate bug class #4 (stealing wlroots lazy-Xwayland forks), and the reaped middle child exits immediately so the wait is genuinely microsecond-scale; sentinel slices `path_buf[0..url.len :0]` and `url_w[0..i :0]` have their terminators set first. `col+7`/`col+8` could theoretically overflow u16 only if cols approached 65535, which is not attainable for a terminal grid — not reported.

HookHandler.zig: parseHookEvent correctly dupes all returned strings before `defer parsed.deinit()` runs (no dangling slices into the freed JSON tree); freeHookEvent covers every variant exhaustively. Only the OOM errdefer gap (finding #4) stands.

HookListener.zig: the MAX_QUEUED=32 ring buffer is correct and achieves full 32-slot capacity via the null-slot discriminator (drop-oldest-on-full frees the evicted event's owned strings); deinit frees all queued events; client fds are non-blocking (ipc.zig:218) so the EAGAIN handling is reachable as intended; `posix.errno` + `std.c.read` (isize, -1 convention, errno global) interplay is correct under libc. socket_path is guaranteed null-terminated because listenPosix rejects path.len >= 108 before init stores it. The three findings above are the substantive issues.

Build/version context: Zig 0.17.0-dev.1417; no API drift found (std.mem.find is present at std/mem.zig:1430). No teruwm frame-scheduling concern applies — HookListener is a teru-terminal feature, not a compositor MCP tool.

- **review:core-pane**: Reviewed all three assigned files line-by-line (Pane.zig 580 lines, Terminal.zig 402, SignalManager.zig 77) plus verified callees/callers: std.posix.poll (EINTR-retrying, so SIGWINCH interruption of the runLoop is NOT a bug), PosixPty.deinit/write/isAlive, Multiplexer pane storage + linkVt relink-after-append, and ServerRestart's initWithPty call site.\n\nSignalManager.zig is clean — the SIGWINCH handler is genuinely async-signal-safe (only ioctl on globals, no alloc/no I/O), the SA.RESTART flag comment is accurate, and updatePtyFd/updateHostFd writes are atomic i32 stores that race-tolerantly interleave with the handler on a single thread.\n\nNo critical/high findings. Notably clean on the flagged known-bug classes: PosixPty.isAlive uses a TARGETED waitpid(pid, WNOHANG) not a global waitpid(-1) (so it can't steal wlroots' lazy-Xwayland fork); respawnShell's double-deinit-on-spawn-failure is safe because Pty.deinit is idempotent (master=-1, child_pid=null, close(-1)/no-kill on second call) and it logs rather than swallowing the error; and the init/initWithPty/initRemote self-referential-pointer-returned-by-value pattern (grid.scrollback -> stack sb, vt.grid/response_ctx -> self) is correctly reconstructed by the mandatory linkVt() call, which Multiplexer re-runs across the whole pane list after every ArrayList.append so realloc can't leave dangling pointers.\n\nAll five reported findings are medium/low with honest reachability caveats — the strongest is the ignored/partial STDOUT write in Terminal.runLoopPosix (real dropped-output in --raw mode; the Win32 path already loops correctly).

- **review:wm-output**: Scope: read all four assigned files in full and verified every suspicion against callers/callees before reporting.

Verified-clean (no findings): ServerListeners.zig is solid — handleNewInhibitor has correct alloc/append rollback (removes listener + destroys tracker on append failure, before incrementing the count) and handleInhibitorDestroy correctly early-returns during shutdown to let Server.deinit own cleanup; all listener links are wl_list_remove'd on teardown (Output.zig:264-266, ServerListeners.zig:147,174; the destroy signal never fires the removed handler). pushOutputManagerState bounds its stack buffer at 16 outputs.

Checked and dismissed: (a) ServerScratchpad.spawn's terminal_panes-full branch is unreachable — terminal_panes.len == max_nodes (256) and each terminal consumes one node + one pane slot, so if addTerminal succeeded there is always a free pane slot. (b) No double-registration: initWithSpawn registers pane_index only; scratchpad.spawn adds the node-registry + terminal_panes entries itself. (c) poll() is bounded (max 4 reads / 8ms budget, level-triggered fd re-fires) so the handleFrame PTY drain does not starve the loop. (d) ServerRepeat timer sources are correctly torn down before wl_display_destroy via Server.releaseTimers (Server.zig:998). (e) refreshAllBorders only touches wayland_surface nodes, so focusScratchpad's terminal-pane border loop is not redundant with it. (f) defaultRect/rectForName arithmetic can't overflow u32 at realistic output sizes and uses saturating subtraction; @intFromFloat on hostile config fractions is user-owned config, not hostile input. (g) wl_output_layout and wlr_scene_output are auto-removed by wlroots' internal per-output destroy listeners, so handleDestroy leaks neither.

No Zig 0.17-dev.1417 stdlib drift found in these files (they are almost entirely wlr/glue extern calls plus std.fmt.bufPrint and std.mem.tokenizeAny, all current).

- **review:core-clip**: Read all three assigned files completely (Clipboard.zig 535L, Selection.zig 451L, KeyHandler.zig 411L) plus callers/callees to verify each claim.\n\nVerified-clean items worth noting (not reported): (1) Clipboard.zig:206 uses std.mem.find, which exists in this stdlib (/usr/lib/zig/std/mem.zig:1430) — no 0.17 drift. (2) sanitise embedded-marker bounds check `i + 5 < src.len` (line 223) is correct, no off-by-one; the trailing-partial-prefix test confirms it. (3) writeSanitised out_buf sizing (max_paste_bytes+16) cannot overflow since sanitise is subtractive-only and the paste_end append is length-guarded (line 151). (4) Selection.getText buffer-full path (lines 193-197) is memory-safe: when pos+line_len>buf.len, avail<line_len<=2048 so both slices stay in bounds; pos never exceeds buf.len. (5) selectWord and getText cannot OOB because Grid.cellAtConst clamps row/col with @min (Grid.zig:390-393). (6) screenToAbsolute additive/saturating-subtract precedence (lines 34,36) parses as intended (Zig additive ops share precedence, left-assoc). (7) fork/exec clipboard helpers reap with targeted waitpid(pid) not waitpid(-1) — no conflict with the wlroots lazy-Xwayland reaper. (8) The teruwm paste READ path is correctly async + CLOEXEC + O_NONBLOCK with mandatory wl_event_source_remove teardown (ServerClipboard.zig), and the wlr_data_source_send fd is correctly NOT closed by us (callee-closes).\n\nThe writeAllToPty severity (high) is honest but bounded: the 1s deadline caps the freeze and the trigger (a stuck PTY reader during a large paste) is uncommon; I flag it because it runs on teruwm's single event-loop thread and the read side was already made async while the write side was not.

- **review:wm-focus**: Audited all three files line-by-line and verified every finding against callees/callers (Node.zig max_nodes=256 and array element types; Server.zig scheduleRender / arrange_scratch_buf=16KB / spawn_table[32][256]u8+len u16 / scratchpad_table[8][32]u8+len u8; TerminalPane.repaintBorderOnly; Workspace.getActiveNodeId/setFocus; vendor/miozu-wlr-glue.c miozu_scene_buffer_commit_dirty; WmConfig gap:u16/border_width:u16/unfocused_opacity:f32; ServerInput.executeAction dispatch has no blanket scheduleRender).\n\nChecked and cleared: refreshAllBorders u16 loop vs max_nodes=256 is safe (256 fits u16, loop exits correctly); all @memcpy in ServerConfig are @min-bounded to the destination array and the following @intCast targets (u16/u8) hold the bound; spawn/scratchpad/action chord appliers self-limit their slot counters; reloadWmConfig and applyConfig both fill WmConfig in place (WmConfig.load / loadWithLibc) and thus correctly avoid the self-referential-slice-returned-by-value dangle (known bug class #8); clearFocusRefs correctly nulls fullscreen_node, focused_terminal/view, grab_node_id, and drag_terminal before teardown; no wl_listener add/remove or scene-node mirroring occurs in these three files (bug classes #2/#6/#7 N/A here); calculateWith `catch return` cannot realistically OOM (256 rects << 16KB FBA). No allocations found in the arrange/focus hot paths.\n\nTop actionable item is the medium-severity opacity gap (finding 1): the unfocused_opacity feature is effectively broken for the Super+J/K focus-cycle and floating/Wayland focus paths, only working on workspace-switch-driven focus.

- **review:wm-mcp-srv**: Read all four files completely and cross-verified against callees/callers and the C glue.

Verified-and-cleared (no finding):
- ServerClipboard copySelection: no stack UAF — miozu_set_clipboard_text (vendor/miozu-wlr-glue.c:919) memcpy's the buffer into a heap wlr_data_source; Selection.getText (src/core/Selection.zig:154) bounds-checks against buf.len; the toast path schedules a frame (Server.setNotification calls scheduleRender).
- ServerClipboard paste/pasteReadable/finishPaste/cancelInflight: correctly non-blocking (drains only currently-available bytes, EAGAIN-terminated, 64KB cap); fds[1] handed to wlr_data_source_send is NOT closed by us (bug class 5 avoided) and the pre-handoff fcntl-failure path DOES close both ends; finishPaste captures len/target before cancelInflight and re-resolves the pane by node_id (no UAF on a pane closed mid-drain); event source removed before display teardown per the documented shutdown ordering. errno handling is correct: std.c.errno(rc) returns SUCCESS unless rc==-1, and std.c.read returns exactly -1 on error.
- ServerProcess: double-fork keeps the intermediate reaped by a targeted waitpid(pid) (not waitpid(-1)), so it never steals wlroots' lazy-Xwayland fork (bug class 4); child path is async-signal-safe (raw syscalls, no alloc/log); closeInheritedFds runs in the child so it can't affect the parent fd table. The parent's blocking waitpid(pid,null,0) is a deliberate, documented, microsecond-bounded reap (intermediate exit(0)s immediately) and matches the established compat.forkExec pattern — considered and not flagged as event-loop starvation.
- Reaper: track/sweep/flushBeforeExec are all correct (WNOHANG throughout, ECHILD releases slots, whole-registry 200ms budget on the pre-execve flush is shared not per-pid); never reaps -1. std.c.W.NOHANG resolves to 0x1 on Linux.
- WmMcpServer: line 106 `@memcpy(self.socket_path[0..path.len], path)` into a 108-byte field is safe because listenPosix (ipc.zig:156) rejects path.len >= 108 before init proceeds; event sources torn down before wl_display_destroy via releaseEventSources with idempotent deinit fallback; emitEventKind bounds math is correct. Minor dead write (emitEventKind writes buf[end+1]=0 but slices [0..end+1]) noted but too trivial to file.

- **review:tiling**: Reviewed all 4 assigned files line-by-line. types.zig and LayoutEngine.zig had no defects worth reporting — both are clean and well-hardened (LayoutEngine.moveNodeToWorkspace has only a theoretical OOM-orphan edge that is unrecoverable and negligible, so I omitted it). layouts.zig and bench.zig account for all findings, all low/info. Key verifications that turned up NO bug (so callers can trust these paths): (1) masterStackN div-by-zero is latent-only — master_count is u8=1, clamped >=1 in adjustMasterCount (Workspace.zig:266), and never restored from session data, so master_count_in==0 is currently unreachable; reported low as a public-API defensive-asymmetry footgun. (2) monocle does not clamp its `active` param but removeNode (Workspace.zig:136-140) keeps active_index < len on every mutation, so no blank-screen bug. (3) threeCol has no reachable div-by-zero for count>=3 (left_count/right_count both >=1, guarded). (4) bench.zig std API usage (Args.Iterator.initAllocator, std.process.Init, Io.Dir) all verified against /usr/lib/zig/std. (5) The zero-alloc render/layout invariant holds: compositor drives calculateWith through a 16KiB FixedBufferAllocator (Server.zig arrange_scratch_buf). No event-loop-blocking, UAF, scene-node-mirror, or global-waitpid patterns exist in these four files (they are pure layout math + an offline benchmark harness).

- **review:wm-small**: Scope: read all 7 assigned files line-by-line and verified suspicions against callers/callees (Server.zig, ServerConfig.zig, WmMcpTools.zig, Keybinds.zig, FontAtlas.zig, Config.zig) and the installed stdlib.\n\nTwo low-severity findings, both verified. No critical/high/medium issues found — the leader engine (LeaderKey), config parser/materializer (LeaderDefs), and font-zoom path (ServerFont) are correct.\n\nExplicitly checked and CLEARED against the assignment's known-bug classes:\n- Bug class 8 (self-referential struct returned by value): ServerFont.zig:31-33 `fa.* = new_atlas` is SAFE — FontAtlas has no self-referential slices; atlas_data/font_data/fallback_font_data are independently heap-allocated and rasterizeAtSize copies font_data (FontAtlas.zig:619-650), so the by-value return + heap-cell overwrite doesn't dangle.\n- Pointer lifetimes in LeaderDefs.Tree.build: label slices point into the stable owning Defs (server.wm_config, a Server field); inter-group slices point into the stable Tree (server.leader_tree, a Server field); the fallback path uses comptime-static CompositorLeader.root_group. All stable for process lifetime. The two-pass slot-alignment (pass 1 caps n at max_entries, pass 2 caps at group_lens[gi]) uses identical skip logic so ei->n mapping matches.\n- Frame-scheduling invariant: applyFontZoom's caller schedules a render (WmMcpTools.zig:141 `if (changed) self.server.scheduleRender()`).\n- Reload keybind accumulation: Keybinds.addBinding dedupes on (mode,mods,key,is_keycode) (Keybinds.zig:604-615), so applyActivate re-adding on reload is idempotent; the stale-old-chord-on-chord-change case is explicitly documented (ServerConfig.zig:332-335).\n- Zig 0.17 drift: std.mem.findScalar (LeaderDefs.zig:142) is the CURRENT non-deprecated name (indexOfScalar is the deprecated alias). std.debug.FullPanic, std.process.Init, wl_event_loop_add_signal usage all build clean.\n- main.zig:51 `Config.load(...) catch Config{...}` is not a live swallow — Config.load internally `catch return config` on every failure path and never actually returns an error, so the catch is unreachable defensive code.\n\nOut-of-scope observation (NOT reported, not in assigned files): WmMcpTools.zig:154 thunkZoomFocused calls tp.zoomFont() without an explicit scheduleRender() the way toolZoom does at line 141 — worth a look by whoever audits WmMcpTools.zig.

- **review:themes-klava**: Read all four files in full. Three of the four are clean with zero findings — I did not pad.\n\nthemes.zig: pure, no alloc/IO; applyBase16Key is fully bounds-checked (`else => unreachable` at :107 is provably unreachable since slot is derived to 0..15). No findings.\n\nkeysym_unicode.zig: generated data table. Verified programmatically (866 entries) that it is strictly ascending by keysym, has no duplicate keysyms, all codepoints fit u21 (<=0x1FFFFF), and none fall in the 0x20-0x7e ASCII-identity range — so keysym.zig:lookupUnicode's binary search is correct. Entries 0xff08..0xffff (control chars) are redundant with named() which shadows them first in label(), but that is harmless. No findings.\n\nkeysym.zig: label() is bounds-safe on every path including buf.len==0 (copy/writeCodepoint/hex-fallback all clamp via @min or catch->copy). Algorithmic range `keysym & 0x00ffffff` is bounded to <=0x10ffff by the `<=0x0110ffff` guard so the @intCast to u21 can't overflow. fkeyName (0xffbe..0xffe0 -> 35 names) and kpDigitName (0xffb0..0xffb9 -> 10 names) indices are guaranteed in range by the switch arms. Verified std.unicode.utf8Encode signature (error{...}!u3) matches the catch usage. No findings.\n\nklava.zig: ring buffer + eviction verified correct for full and capacity==1 cases; every @memcpy (feed :162, displayText :105, append :238) is length-matched and can't overrun; count uses saturating +|=; nextDeadlineMs/@max and pruneExpired agree on the oldest-expires-first ordering. Two low/info items reported. No event-loop/UAF/wl_listener concerns — these are pure data structures with no I/O, timers, allocations, or wlr node mirroring.\n\nConsumer cross-check (src/compositor/KeysOsd.zig): feed() calls render + armTimer + server.scheduleRender() (frame scheduled after state change, invariant honored); onTimer prunes then scheduleRender(). This mitigates the finding-1 resurrection path in the live compositor.\n\nNo Zig 0.17-dev.1417 API drift found; the files compile clean on this toolchain (utf8Encode, bufPrint, @splat-on-array all current).

- **review:mcp-tools**: Read all three files completely (McpTools.zig 515 lines, in_band.zig 405, McpBridge.zig 385) plus callers (WmMcpTools.zig, McpFramework.zig:143-343, McpServerTools.zig, McpServer.zig:255-309, ipc.zig) and the installed stdlib (mem.zig, fmt.zig).\n\nHeadline: McpTools.extractNestedJsonBool has an out-of-bounds slice on its whole-json fallback path, reachable from the teruwm MCP socket via a crafted tools/call that places a boolean key before \"arguments\". Because dispatch runs on the single wl_event_loop thread, it panics (Debug/ReleaseSafe) or reads OOB (ReleaseFast, the shipping build) and takes down the compositor. extractNestedJsonInt has the identical wrong-offset bug but is self-guarded against the OOB (returns null/garbage instead of crashing). extractNestedJsonString is the correct reference implementation (it slices json[args_pos..] and deliberately does not fall back).\n\nin_band.zig is clean: the pane-scoping guard (forced pane_id emitted first + isPlainKey rejecting structural keys + buildJsonRpcRequest bounds checks + frameDcs bounds check) all hold, and the int-fallback bug does not weaken it because the forced pane_id sits inside \"arguments\" so the primary suffix search always wins. handleQuery's ~40 KiB of stack buffers and synchronous mcp.dispatch are fine given the read-only allowlist (no blocking I/O).\n\nThe rest are low-severity robustness/maintainability items in McpBridge.zig (unchecked partial writes, duplicated read-only list, string-unaware brace matcher, per-byte reads). No zig-0.17 API drift found in these files — every std call used (find/findPos/findScalarLast/bufPrintSentinel/parseInt/startsWith) matches the installed stdlib.

- **cc:event-loop**: Focused event-loop-latency sweep of all src/compositor/*.zig + vendor/miozu-wlr-glue.c. The codebase is unusually disciplined about the single-threaded wl_event_loop: PTY drain is bounded (4 reads / 8ms, TerminalPane.zig:379), clipboard foreign-source paste is async via a WL_EVENT_READABLE fd source (ServerClipboard.zig:129, non-blocking read loop), the mouse-path gesture is timer-driven not sleep-driven (ServerMouse.zig, the fix from the mousepath-async memory), bar exec widgets fork + read non-blocking (Bar.zig:429), MCP event push uses O_NONBLOCK writes (WmMcpServer.zig:244), and every timer callback (barTick DPMS/signature-gated, notify tick, repeat timers, keys OSD) does bounded work. All blocking waitpid calls are WNOHANG (Reaper.zig) except the two double-fork immediate-child reaps (ServerProcess.zig:50, glue 786/899) which cost microseconds because the reaped child _exit(0)s right after forking the real worker. Reaper.flushBeforeExec's sleepNs loop is exec-path-only and 200ms-bounded. main.zig's run path is a bare wl_display_run with no blocking. Four residual vectors reported: (1) synchronous screenshot composite+readback+PNG-encode on the loop, resolution-scaled worst case — the explicitly-flagged GPU-readback class; (2) a reachable u32 overflow in the mouse-path sample calc (crash in safety builds); (3) the MCP request read loop's stall counter reset that lets a slow client hold the loop; (4) an info note that the per-pane PTY parse budget doesn't compose across panes. No UAF, listener-teardown, or partial-damage regressions found in the event-loop paths — the known bug classes from prior incidents all have current guards in place with explanatory comments.

- **cc:lifecycle-leaks**: Scope: read the full teardown surface end-to-end on Zig 0.17.0-dev.1417 — Server.zig (initOnHeap/initFields/deinit/releaseSeat/releaseTimers/releaseEventSources/destroyXwayland), ServerConfig.applyConfig, Bar.zig, TerminalPane.zig, Node.zig, XdgView.zig, XwaylandView.zig, Output.zig, ServerInput.zig (Keyboard), WmMcpServer.zig, Reaper.zig, ServerListeners (InhibitorTracker), ServerCursor (PointerConstraint), ServerScreenshot, ServerRestart, LayoutEngine/Workspace, and SoftwareRenderer/FontAtlas/BarWidget for adopted-vs-owned buffers.

Confirmed the 3 KNOWN leaks (findings 1-3: Server struct, FontAtlas+variants, Bar struct) and found 3 OTHERS the headless report couldn't surface because that shutdown had zero input devices and zero panes (findings 4-6: Keyboard structs, open TerminalPanes+Pane heap, LayoutEngine workspaces' node_ids). All six are process-exit-only leaks (the OS reclaims), so no running-session growth; their practical cost is polluting the debug-allocator shutdown report and masking future regressions. All are category=memory, marked high per the given rubric (leak = high).

VERIFIED-CORRECT (not reported, as instructed): the just-added Server.releaseEventSources → WmMcpServer.releaseEventSources path is correct and complete — both socket event sources are removed and nulled before wl_display_destroy (main.zig defer at :109), and WmMcpServer.deinit's fallback removals no-op on the nulled fields before closing fds/unlinking sockets/destroy(self). Bar.deinitExecs is likewise idempotent (pe.event_source nulled). No UAF there.

Also checked and found CLEAN (no leak/UAF): XdgView.handleDestroy removes all 9 listeners + destroys view; XdgView.Popup handlePopupDestroy; XwaylandView.handleDestroy removes all 7 listeners + destroys the still-live scene tree so the armed scene_destroy_listener can't fire into freed memory; Node.zig by_id freed via deinitIndex, border rects are scene-tree children reaped recursively (mirrors nulled in handleSceneTreeDestroy — the prior UAF class is guarded); Output.handleDestroy stays armed through backend destroy so Output structs are freed (asymmetric with keyboards, which is exactly why finding 4 exists); InhibitorTracker freed at both runtime and shutdown with a shutting_down double-free guard; PointerConstraint freed via client teardown; Reaper uses a fixed 128-slot static array with bounded waits (no heap, no event-loop starvation); ServerScreenshot and ServerRestart allocations all have defer free. SoftwareRenderer.init allocates only the framebuffer, which Bar/KeysOsd/LeaderPanel all correctly adopt from wlr memory (init alloc freed, re-pointed) — so those overlays leak no Zig heap. LeaderDefs.Tree / WidgetList / Launcher / wm_config are fixed-buffer, no heap.

Fix-ordering caveats worth heeding: finding 5 (panes) MUST be freed before wl_display_destroy since TerminalPane.deinit touches scene nodes; findings 1/2/3/6 are pure Zig heap and safe in deinit (after display destroy). Finding 4's fix should live inside the existing releaseSeat keyboard loop to preserve the intentional pre-backend-destroy listener detach.

- **cc:render-damage**: Scope: render + damage hot path, both binaries. Read in full: software.zig, Grid.zig (render-relevant), Multiplexer.zig, windowed.zig, Compositor.zig, TerminalPane.zig, FontAtlas.zig, Bar.zig, Output.zig, TuiRenderer.zig, TuiScreen.zig (head), plus Server/ServerCursor scheduling and LayoutEngine.calculate.\n\nZero-alloc-in-render-hot-path invariant: HOLDS for the pixel software renderer (software.zig renderRangeSel/blitGlyphRow — no alloc/format), for Compositor.renderPaneIntoRect, and for the teruwm TerminalPane path (framebuffer adopted from wlr buffer; FBA for rects). glyphSlot/getGlyph are O(1) range lookups, no hashmaps, no alloc. VIOLATION found only in the TUI path (finding #5, per-frame heap []Rect via calculate()).\n\nThe two KNOWN issues named in the brief are CONFIRMED and quantified: (1) Multiplexer.renderAllWithSelection full-memsets + repaints every cell every frame ignoring grid.dirty_row_min/max = findings #1 (full repaint), #3 (redundant memset double-write), #4 (blink opt nullified); (2) windowed.zig gates the 60fps cap only on the idle sleep, not the render itself = finding #2 (verified: no time gate at windowed.zig:1529; readAndProcess reads one 8KB chunk/pass so render rate tracks PTY throughput under a flood).\n\nNo crash/UAF/leak/event-loop-starvation defects surfaced in the render+damage path this pass — the known wlr-scene-node-mirror UAF class, targeted-reaper, callee-closes-fd, and listener-teardown patterns all looked intact where they intersect rendering. Findings are all perf-waste (medium) or idiom (low), plus one info record that the teruwm path is sound. Highest-value fix by far is #1 (adopt dirty-row rendering in the shared Compositor/Multiplexer path); #2 (render-rate cap) is the natural companion. All findings are read-verified with exact file:line; no code was changed.

- **cc:error-handling**: Sweep scope: all 117 .zig files under src/. Grepped every requested pattern: `catch {}` (54 hits), `catch unreachable` (12), bare `unreachable` (6 code sites), `@panic` (1), `orelse unreachable` (4), plus `try` inside every deinit/destroy/cleanup/remove/free function (48 such functions checked via brace-matched body extraction). Also swept for `catch @panic`, `catch |e| ... unreachable`, and `=> @panic` (none found). Classification below; I read the actual code + adjacent comments for each before deciding.\n\nDANGEROUS (reported): 1 finding — ServerRestart.zig:32 @panic (low, above).\n\nFINE — verified truly infallible / test-only / guarded-invariant, NOT reported:\n- All 12 `catch unreachable` are either inside `test` blocks (compat.zig:355-365, McpTools.zig:465-479, Scrollback.zig:842, ProcessGraph.zig:454-477) or bufPrint of a fixed static string into an amply-sized buffer (teruwmctl/main.zig:635 — ~90 bytes into [512]).\n- `orelse unreachable`: SessionDef.zig:884-890 are in tests; platform/macos/platform.zig:41 is objc_getClass on hard-coded framework class names (always present on supported macOS; macOS is a secondary target).\n- Bare `unreachable`: themes.zig:107 (`slot` provably 0x00-0x0F from a single hex digit, all 16 arms covered); KeyHandler.zig:360 (comptime test blk); XdgView.zig:142 `.tiled => unreachable` (guarded by outer `if (placement != .tiled ...)`); WmMcpTools.zig:509 `.empty => unreachable` (guarded by `if (kind[slot] != .empty)`); ServerCursor.zig:657 & McpServer.zig:297 are a comment and a string literal, not code.\n- No `try` inside any deinit/destroy/cleanup — that shipped bug class is absent.\n- VtParser.zig (the pure, untrusted-terminal-data parser) is CLEAN: zero unreachable / @panic / catch{} / orelse unreachable. No asserts in src/compositor either. The 'parser fed untrusted client data hits unreachable' bug class was not found.\n- daemon.zig:651 is the FIXED version of a past silent-catch bug — it now logs saveSession errors (the `catch {}` comment there is historical documentation, the real code logs).\n\nDELIBERATE-BUT-NOT-REPORTED (soft-fail on OOM, per the codebase's documented degradation philosophy; the task's 'dangerous' form is OOM-as-*unreachable*/crash, whereas these are OOM-as-catch{}/no-crash):\n- The dominant `catch {}` pattern is best-effort ptyWrite to a possibly-dead child (in_band 156/191, Pane 261, Clipboard 208, KeyHandler 168/170, mouse.zig x6, TerminalPane 623, daemon 418/425, windowed 596/612/877/1111/1113/1144, SessionDef 383) and best-effort kill() of possibly-dead pids (PosixPty 165/167/199, Reaper 80) — correct to drop.\n- OOM-only inconsistency drops: registry/layout inserts `catch {}` (McpServerTools 437, TerminalPane 222/331/354, Node 135/158, XdgView 572, ServerLayout 238, ServerWindow 96). I verified addNode/addNodeSplit (Workspace.zig:116/318) only fail via ArrayList.append OOM. Worth noting as a fragility: createRestored (TerminalPane:331) does `addNode ... catch {}` and still registers the pane in terminal_panes + pane_index, whereas the sibling createWithSpawn (:240) does `catch return null` — so under OOM the restored path can leave a pane rendered+tracked but absent from the layout tree (untileable). OOM-only, so not reported as a finding, but the two paths disagree on failure semantics.\n- windowed.zig:104 `posix.poll(...) catch {}`: given the project's CPU-drain sensitivity I checked for a busy-spin risk; poll here can only realistically fail on EINTR (transient), so no persistent spin. Low/none.\n- tier.zig:53 and tui.zig:352 resize `catch {}` are documented 'keep old size on OOM rather than crash'.\n\nOverall: this tree has clearly already been through an error-handling audit (many catch sites carry regression-referencing comments). The one net-new item is the ServerRestart @panic.

- **cc:security**: Scope: audited the six named surfaces in the main source tree only (ignored .claude/worktrees/ copies). Zig 0.17 API usage checked against std where relevant.\n\nClean surfaces (zero findings, verified by reading the code): (a) VtParser.zig hostile-byte handling — param cap MAX_PARAMS=16 with idx bounds checks, OSC cap MAX_OSC_LEN=256 (overflow truncates), DCS `.dcs_st_esc` isolation prevents embedded-ESC leaking into CSI, `@min`/`@truncate` clamps on ED/EL/SU/SD/REP and the u21 char decode avert ReleaseSafe panics, feed() guards a 0x0 grid, DA/DSR responders guard echo loops; the OSC-8 hyperlink index is a u8 into a [256] array so `+%=` can never go OOB. (b) persist/Scrollback.zig decodeKeyframe is fully bounds-checked (decodeColorBounded, per-read length guards, @truncate char) AND the keyframe binary format is only ever in-memory — scrollback restore replays a VT byte stream through the (bounded) parser, so there is no on-disk untrusted-keyframe parse path. (c) persist/Session.zig deserialize caps node_count at 100k, length-prefixes with readAll==len checks, and has correct errdefer cleanup (a regression test covers the truncated-role double-free). (d) teruwmctl + forward.zig: verbs validated to [A-Za-z0-9_], all positional args JSON-escaped (control chars rejected), no shell exec anywhere; forwardRequest bounds the 64K read and checks payload.len>out.len. (e) compositor/Session.zig .tsess restore execvp's the saved cmd, but the session name is sanitized by compat.isSafeFilename (rejects '/','\\','..',NUL) so no path traversal, and it is same-UID user files regardless. (f) Screenshot writes: isSafeScreenshotPath rejects '..'/NUL and uses boundary-correct pathUnderDir (narrow allowlist /tmp, ~/Pictures, ~/.config/teru/screenshots), png.write uses openFileNoFollow (O_NOFOLLOW|O_CREAT|O_TRUNC,0600). (g) ServerClipboard paste correctly does NOT close the wlr_data_source_send fd (callee-closes / Xwayland INCR), sets O_NONBLOCK+CLOEXEC, caps at 64K, and tears down the event source in cancelInflight before display destroy. (h) McpFramework handleRequestFd bounds the partial-read stall at 8x5ms=40ms (no multi-second loop block) and read-only allow/blocklist gating (incl. the forward gate for teruwm_*) is correct and tested.\n\nOne accepted-tradeoff observation (not filed): WmMcpServer handles each MCP request synchronously on the wl_event_loop thread, so a malicious same-UID client that connects-but-stalls can block the compositor loop up to 40ms per connection; repeated across many connections this is a modest same-UID input-jank DoS. It is bounded and same-UID (who has many stronger DoS options), and the 40ms cap is a deliberate, documented improvement over the prior 2s — noting for awareness only.

- **cc:architecture**: Scope covered: A1 focus authorities (Server focus triple + focused_output + LayoutEngine.active_workspace + Workspace.active_node/active_index/master_id + fullscreen_node/grab_node_id/drag_terminal), A4 twin-table (Node registry vs Workspace.node_ids and vs wlroots scene tree), the three named duplication candidates, and the 8 known shipped bug classes.\n\nBug-class hunt results (verified by reading, not padded): class 1 (event-loop blocking) — clean; the only unbounded-looking loops (ServerClipboard.zig:148 paste drain, Reaper.zig sweep) are non-blocking (O_NONBLOCK + EAGAIN return) or NOHANG. class 4 (waitpid(-1)) — clean; all reaping is targeted per-pid (Reaper.zig, ServerProcess.zig:50, compat.zig), with explicit comments defending it. class 5 (data_source_send fd) — correct; ServerClipboard.zig:114-120 lets the callee close and documents the Xwayland INCR hazard. class 6 (event sources before display destroy) — correct; ServerClipboard.cancelInflight and Bar exec teardown are ordered before wl_display_destroy with comments. class 7 (listener add/remove balance) — clean; XdgView 14 add / 14 remove (the '15' from grep is a comment), XwaylandView 8/8, Output 3/3, ServerCursor 1/1; Server's 22-add/5-remove is expected (process-lifetime globals). class 2 (scene-node mirror UAF) — one asymmetry found and reported (XdgView lacks the border_rects-nulling scene_destroy_listener XwaylandView has; currently safe via wlroots unmap-before-destroy). class 3 (full-buffer GPU re-upload) — not re-audited in depth this pass; the codebase's own memory notes record it as fixed (partial-damage + signature gating in Bar/TerminalPane).\n\nOverall the compositor is unusually well-hardened — nearly every teardown/focus path carries a comment tracing a specific past coredump, and the focus-write consolidation onto Workspace.setFocus (A1) and the urgent-count/by_id incremental indexes (A4) are solid. The remaining structural risks are the still-live SECOND authorities that setFocus/moveSlotToWorkspace don't cover: the legacy active_workspace (desyncs on output hotplug — the one concrete bug), the unguarded Node.workspace vs node_ids pairing, and the swap/rotate family bypassing setFocus. Highest-value cheap wins: the one-line active_workspace sync in Output.handleDestroy, unifying the three color parsers, and pruning the 435 MB of stale worktrees. All findings cite verified file:line; nothing here is speculative beyond the explicitly-caveated XdgView ordering dependency. Build/tests were reported green on this toolchain and I did not re-run them.

- **zig:build-system**: Scope: build.zig (full), build.zig.zon, Makefile, tools/fix-crt.sh, tools/zig-lib-fix.sh — audited against installed std at /usr/lib/zig (0.17.0-dev.1417+20befa4e6).

BUILD API: zero drift. Every Build call in build.zig matches the installed std — addExecutable/addLibrary/addTest take ExecutableOptions/LibraryOptions/TestOptions (Build.zig:710/766/804) with `.name`+`.root_module`; createModule takes Module.CreateOptions where `strip` (Module.zig:25) and `link_libc` (Module.zig:19) are valid `?bool` fields; `exe.lto = .full` matches Compile.zig:180 (`lto: ?std.zig.LtoMode`); Step.Options.addOption(comptime T, name, value) matches Options.zig:43; Run.addPassthruArgs matches Run.zig:573; installArtifact/addRunArtifact/standardTargetOptions/standardOptimizeOption/option/step/path all present with matching signatures. No deprecated Step/Module patterns, no addModule-vs-createModule confusion, option wiring (build_options -> addOptions on every module) and the test-step fan-in (test_step dependOn run_lib_tests + run_wm_tests) are correct. build.zig itself produced no findings — a clean file.

WORKAROUND VERDICT: both scripts are obsolete on dev.1417.
- .sframe CRT gap is CLOSED (proved two ways: `-lc` hello-world without --libc linked the sframe crt1.o and ran; full plain `zig build` succeeded, exit 0). fix-crt.sh runs unconditionally and does real work every build for no benefit, and has additionally drifted (hardcoded GCC 15.2.1 gone).
- termios `arch_bits == .alpha` typo is FIXED upstream (grep exit 1); zig-lib-fix.sh self-heals to an empty no-op exactly as designed — confirmed harmless.

Method: read installed std for API + the alpha typo; compiled/ran a hello-world in scratchpad; ran a full `zig build`; exercised zig's `--libc` arg parser to confirm the run-wm breakage in both cache states. Absolute repo path: /home/ng/code/workbench/foss/teru (findings cite repo-relative paths per the audit convention).
